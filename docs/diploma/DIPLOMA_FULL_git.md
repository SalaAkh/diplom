# ОБЛОЖКА

ҚАЗАҚСТАН РЕСПУБЛИКАСЫ БІЛІМ ЖӘНЕ ҒЫЛЫМ МИНИСТРЛІГІ

МИНИСТЕРСТВО НАУКИ И ВЫСШЕГО ОБРАЗОВАНИЯ РЕСПУБЛИКИ КАЗАХСТАН

«Қарағанды инновациялық технологиялар колледжі» КЕАҚ

НАО «Карагандинский колледж инновационных технологий»

## ДИПЛОМДЫҚ ЖОБА / ДИПЛОМНЫЙ ПРОЕКТ

**Тақырыбы / Тема:** «Жеке қалауларды талдауға және даму бағыттарын анықтауға арналған өзін-өзі тану цифрлық жүйесін жасау» / «Разработка цифровой системы самопознания для анализа личностных предпочтений и направлений развития»

**Білім беру бағдарламасы / Образовательная программа:** 06130100 «Программное обеспечение»

**Біліктілігі / Квалификация:** 4S06130103 «Техник-программист»

**Орындаған / Выполнил:** Ахмедьянов Саламат Ержанұлы

**Тобы / Группа:** КПО 9/22-2

**Жетекшісі / Руководитель:** [ФИО руководителя]

Қарағанды, 2026 / Караганда, 2026

---

# ТИТУЛЬНЫЙ ЛИСТ

ҚАЗАҚСТАН РЕСПУБЛИКАСЫ БІЛІМ ЖӘНЕ ҒЫЛЫМ МИНИСТРЛІГІ

МИНИСТЕРСТВО НАУКИ И ВЫСШЕГО ОБРАЗОВАНИЯ РЕСПУБЛИКИ КАЗАХСТАН

«Қарағанды инновациялық технологиялар колледжі» КЕАҚ

НАО «Карагандинский колледж инновационных технологий»

«Қорғауға жіберілді»
«Допущен к защите»

Бөлім меңгерушісі / Зав. отделением
_____________ [ФИО зав. отделением]
«____» _____________ 2026 ж. / г.

## ДИПЛОМДЫҚ ЖОБА / ДИПЛОМНЫЙ ПРОЕКТ

**Тақырыбы:** «Жеке қалауларды талдауға және даму бағыттарын анықтауға арналған өзін-өзі тану цифрлық жүйесін жасау»

**Тема:** «Разработка цифровой системы самопознания для анализа личностных предпочтений и направлений развития»

**Білім беру бағдарламасы / Образовательная программа:** 06130100 «Программное обеспечение»

**Біліктілігі / Квалификация:** 4S06130103 «Техник-программист»

**Орындаған / Выполнил:** _____________ Ахмедьянов С.Е.

**Тобы / Группа:** КПО 9/22-2

**Жетекшісі / Руководитель:** _____________ [ФИО руководителя]

Қарағанды, 2026 / Караганда, 2026

---

# ЗАДАНИЕ НА ДИПЛОМНЫЙ ПРОЕКТ

**Студент:** Ахмедьянов Саламат Ержанұлы

**Группа:** КПО 9/22-2

**Образовательная программа:** 06130100 «Программное обеспечение»

**Тема дипломного проекта:** «Разработка цифровой системы самопознания для анализа личностных предпочтений и направлений развития»

**Утверждена приказом №____ от «____» _____________ 2026 г.**

**Исходные данные к работе:**
Разработать прогрессивное веб-приложение (PWA) для анализа личностных предпочтений пользователя по 6 измерениям на основе интерактивных сценариев-дилемм. Приложение должно работать полностью в браузере без серверной части, поддерживать три языка (казахский, русский, английский) и визуализировать результаты с помощью радарных и столбчатых диаграмм.

**Содержание расчётно-пояснительной записки (перечень подлежащих разработке вопросов):**

1) Теоретические основы разработки системы самопознания: психология принятия решений, модели анализа личности, цифровые инструменты самопознания, визуализация данных, прогрессивные веб-приложения.

2) Проектирование системы: анализ требований, модель анализа по 6 измерениям, алгоритмы нормализации оценок, структура данных, архитектура приложения, проектирование интерфейса.

3) Реализация системы: выбор технологий, модуль сценариев и тестирования, модуль анализа личности, модуль визуализации, дополнительные модули.

4) Экспериментальная часть: методика тестирования, результаты тестирования, анализ полученных данных, ограничения и перспективы.

**Перечень графического материала:**
а) архитектура системы;
б) диаграммы классов;
в) блок-схемы алгоритмов;
г) скриншоты интерфейса;
д) примеры радарных диаграмм.

**Срок сдачи законченного проекта:** «____» _____________ 2026 г.

**Руководитель:** _____________ [ФИО руководителя]

**Задание принял к исполнению:** _____________ Ахмедьянов С.Е.

**Дата:** «____» _____________ 2026 г.


---

# АҢДАТПА

Дипломдық жоба пайдаланушының жеке қалауларын талдауға және даму бағыттарын анықтауға арналған өзін-өзі тану цифрлық жүйесін жасауға арналған.

Жұмыстың мақсаты — интерактивті таңдау сценарийлері арқылы пайдаланушының жеке қалауларын талдауға және даму бағыттарын анықтауға арналған цифрлық жүйе жасау.

Жоба барысында жеке қалауларды 6 өлшем бойынша талдау моделі жасалды: ұтымдылық, бақылау, стратегиялық ойлау, зерттеу әлеуеті, дара бастамашылдық және мағына іздеу. Модель шешім қабылдау теориясына негізделген және сценарий-дилеммалар арқылы жүзеге асырылған.

Жүйе HTML5, CSS3 және JavaScript ES6+ технологияларын қолдана отырып прогрессивті веб-қосымша (PWA) түрінде жасалған. Нәтижелерді визуализациялау үшін Chart.js кітапханасы қолданылды. Деректер localStorage арқылы пайдаланушының браузерінде толықтай жергілікті түрде сақталады.

Жүйеде тестілеудің үш түрі жүзеге асырылды: базалық сценарийлік тест (10 дилемма), кеңейтілген тест (шкалалық, ашық, ситуациялық сұрақтар) және когнитивтік тест. Нәтижелер радарлық және бағандық диаграммалар арқылы визуализацияланады.

Қосымша мүмкіндіктер: кәсіби рөлдермен салыстырмалы талдау, атақты тұлғалармен салыстыру, профиль эволюциясын бақылау, HTML-есеп экспорты, 3 тілді қолдау (қазақ, орыс, ағылшын).

Жүйе функционалдық, кросс-браузерлік және бейімділік тестілеуден сәтті өтті.

Түсіндірме жазбаның көлемі — ХХ бет, суреттер — ХХ, кестелер — ХХ, әдебиет көздері — 25.

# АННОТАЦИЯ

Дипломный проект посвящён разработке цифровой системы самопознания, предназначенной для анализа личностных предпочтений и определения направлений развития пользователя.

Целью работы является разработка цифровой системы для анализа личностных предпочтений и определения направлений развития пользователя через интерактивные сценарии выбора.

В ходе работы была разработана модель анализа личностных предпочтений по 6 измерениям: рациональность, контроль, стратегическое мышление, исследовательский потенциал, индивидуализм и поиск смысла. Модель основана на теории принятия решений и реализована через интерактивные сценарии-дилеммы.

Система реализована в виде прогрессивного веб-приложения (PWA) с использованием технологий HTML5, CSS3 и JavaScript ES6+. Для визуализации результатов применена библиотека Chart.js. Данные хранятся полностью локально в браузере пользователя через localStorage.

В системе реализованы три типа тестирования: базовый сценарный тест (10 дилемм), расширенный тест (шкальные, открытые, ситуационные вопросы) и когнитивный тест. Результаты визуализируются с помощью радарных и столбчатых диаграмм.

Дополнительные функции: сравнительный анализ с профессиональными ролями, сравнение со знаменитыми личностями, отслеживание эволюции профиля, экспорт результатов в HTML-отчёт, поддержка 3 языков (казахский, русский, английский).

Система успешно прошла функциональное, кроссбраузерное и адаптивное тестирование.

---

# ВВЕДЕНИЕ

Актуальность темы исследования. В современном обществе, характеризующемся стремительным развитием информационных технологий и многообразием жизненных путей, способность человека к осознанной самооценке и пониманию собственных предпочтений приобретает особое значение [1]. Потребность в инструментах самопознания обусловлена несколькими факторами: ростом числа доступных карьерных траекторий, увеличением объёма принимаемых решений и необходимостью осознанного выбора направлений личностного и профессионального развития.

Психология принятия решений, основы которой были заложены в работах Д. Канемана и А. Тверски [1, 2], показала, что стиль принятия решений является устойчивой личностной характеристикой. Анализ индивидуальных паттернов мышления через поведенческие выборы позволяет получить объективную картину предпочтений, не подверженную влиянию социальной желательности ответов, характерному для традиционных личностных опросников [3].

Существующие цифровые инструменты анализа личности обладают рядом ограничений. Онлайн-тесты на основе типологий (MBTI, соционика) сводят многообразие личности к дискретным типам, не отражая непрерывной природы психологических характеристик [4]. Большинство платформ требуют передачи данных на сервер, что создаёт риски нарушения конфиденциальности. Кроме того, традиционные тесты основаны на прямых вопросах и не моделируют реальные ситуации принятия решений.

Данные обстоятельства обусловливают необходимость разработки нового подхода к цифровому самопознанию — системы, основанной на анализе поведенческих выборов в контексте ситуационных дилемм, с полной локальной обработкой данных и представлением результатов в виде непрерывного спектра по нескольким измерениям.

Цель работы — разработать цифровую систему для анализа личностных предпочтений и определения направлений развития пользователя через интерактивные сценарии выбора.

Для достижения поставленной цели необходимо решить следующие задачи:

1) изучить существующие подходы к анализу личности и предпочтений;
2) разработать модель анализа паттернов мышления по 6 измерениям;
3) спроектировать архитектуру веб-приложения и структуру данных сценариев;
4) реализовать модули тестирования, анализа и визуализации результатов;
5) реализовать систему категоризированных рекомендаций по направлениям развития;
6) обеспечить многоязычную поддержку (казахский, русский, английский);
7) провести тестирование системы и оценить результаты.

Объект исследования — цифровые системы самопознания и анализа личностных предпочтений.

Предмет исследования — методы анализа личностных предпочтений через интерактивные сценарии выбора и визуализацию результатов.

Методы исследования:

- анализ научной литературы по психологии принятия решений;
- моделирование и проектирование программной архитектуры;
- программирование и разработка веб-приложения;
- тестирование и экспериментальная проверка;
- визуализация и статистический анализ данных.

Теоретическая база исследования включает работы Д. Канемана и А. Тверски по психологии принятия решений [1, 2], модели анализа личности Big Five [5] и MBTI [6], теорию самодетерминации Э. Деси и Р. Райана [7], методологию визуализации данных [8] и стандарты разработки прогрессивных веб-приложений [9].

- в качестве личного инструмента саморефлексии и самоанализа.

---

# ОПРЕДЕЛЕНИЯ, ОБОЗНАЧЕНИЯ И СОКРАЩЕНИЯ

В данной дипломной работе применяются следующие термины с соответствующими определениями и сокращениями:

**API** (Application Programming Interface) — программный интерфейс приложения.
**CSP** (Content Security Policy) — политика защиты контента.
**DOM** (Document Object Model) — объектная модель документа.
**FCP** (First Contentful Paint) — время первой отрисовки контента.
**HTML** (HyperText Markup Language) — язык гипертекстовой разметки.
**JSON** (JavaScript Object Notation) — текстовый формат обмена данными.
**LCP** (Largest Contentful Paint) — время отрисовки самого крупного элемента.
**PWA** (Progressive Web Application) — прогрессивное веб-приложение.
**SPA** (Single Page Application) — одностраничное веб-приложение.
**SW** (Service Worker) — сервис-воркер, скрипт, работающий в фоновом режиме.
**SUS** (System Usability Scale) — системная шкала удобства использования.
**UI** (User Interface) — пользовательский интерфейс.
**UX** (User Experience) — пользовательский опыт.
**WCAG** (Web Content Accessibility Guidelines) — руководство по доступности веб-контента.

---

Структура работы. Дипломный проект состоит из введения, четырёх глав, заключения, списка использованной литературы и приложений.

В первой главе рассмотрены теоретические основы: психология принятия решений, модели анализа личности, обзор цифровых инструментов самопознания, методы визуализации данных и технология прогрессивных веб-приложений.

Во второй главе описано проектирование системы: анализ требований, разработка модели анализа по 6 измерениям с алгоритмами нормализации, проектирование структуры данных, архитектуры приложения и пользовательского интерфейса.

В третьей главе представлена реализация системы: выбор технологий, реализация модулей тестирования, анализа, визуализации и дополнительных компонентов, а также примеры работы системы.

В четвёртой главе описана экспериментальная часть: методика тестирования, результаты тестирования, анализ работы системы, ограничения и перспективы развития.


---

# 1 ТЕОРЕТИЧЕСКИЕ ОСНОВЫ РАЗРАБОТКИ СИСТЕМЫ САМОПОЗНАНИЯ

## 1.1 Психология принятия решений и анализ предпочтений

Исследования в области психологии принятия решений составляют теоретический фундамент разрабатываемой системы. Центральное место занимает двухсистемная модель мышления, предложенная лауреатом Нобелевской премии Д. Канеманом [1]. Согласно этой модели, человеческое мышление функционирует посредством двух автономных, но взаимодействующих систем:

- **Система 1 (Интуитивная)** работает автоматически и быстро, практически не требуя усилий и не давая ощущения сознательного контроля. Она генерирует сложные паттерны идей, но только Система 2 может выстроить их в упорядоченную последовательность шагов. Именно Система 1 отвечает за быстрые суждения на основе опыта и ассоциаций. В нашей системе это соответствует полюсу «Интуиция».
- **Система 2 (Аналитическая)** выделяет внимание, необходимое для сознательных умственных усилий, в том числе для сложных вычислений. Она ассоциируется с субъективным опытом владения собой, выбора и концентрации. Система 2 способна перепрограммировать работу Системы 1, изменяя её автоматические функции внимания и памяти. В нашей системе это соответствует полюсу «Рациональность».

Данное разделение легло в основу одного из ключевых измерений разрабатываемой системы — шкалы «Рациональность ↔ Интуиция». Пользователи, предпочитающие систематический анализ ситуации перед принятием решения, тяготеют к полюсу рациональности; пользователи, опирающиеся на чутьё и целостное восприятие, — к полюсу интуиции.

А. Тверски и Д. Канеман в серии исследований [2] показали, что при принятии решений люди систематически отклоняются от рациональных моделей, используя когнитивные эвристики — упрощённые правила обработки информации, которые часто приводят к систематическим ошибкам (когнитивным искажениям). Эти ментальные "ярлыки" эволюционно оправданы: они снижают энергетические затраты мозга и позволяют быстро реагировать в условиях неопределенности. Тем не менее, в сложной современной среде, где решения требуют многофакторного анализа, эти же механизмы становятся источником нерационального поведения.

К основным эвристикам, влияющим на стиль принятия решений, относятся:

а) **Эвристика доступности** — оценка вероятности или частоты события по легкости, с которой примеры приходят на ум. Если какое-то событие (например, инновационный успех или громкий провал стартапа) широко обсуждается, люди склонны переоценивать вероятность повторения такого исхода. В сценариях разрабатываемой системы это проявляется в выборе вариантов, которые кажутся «очевидными» из-за их медийной или культурной популярности. Анализ частоты выбора таких вариантов позволяет оценить склонность пользователя к внешнему влиянию.

б) **Эвристика представительности** — суждение о вероятности того, что объект А принадлежит к классу Б, на основе того, насколько А похож на типичного представителя класса Б. Это часто ведет к игнорированию статистической вероятности (априорной частоты) и пренебрежению размером выборки. В нашей системе это выявляется через сценарии, предлагающие выбор между «типичным» (эмоционально привлекательным) и «аналитическим» (статистически обоснованным) решением.

в) **Эвристика привязки и корректировки (эффект якоря)** — тенденция полагаться на начальную информацию (якорь) при вынесении суждений. Даже если якорь случаен или нерелевантен, он оказывает мощное влияние на последующую оценку масштабов или вероятностей. Сценарии системы используют этот эффект для анализа склонности пользователя к конформизму, жесткости мышления или, напротив, к независимой оценке фактов.

г) **Иллюзия контроля** — когнитивное искажение, выражающееся в тенденции людей верить в то, что они могут контролировать, или по крайней мере влиять на результаты событий, на которые они на самом деле не оказывают осмысленного воздействия. Это искажение формирует базис для измерения "Контроль ↔ Адаптация" в нашей модели анализа.

Эти эвристики непрерывно проявляются в реальных повседневных ситуациях выбора и могут быть зафиксированы только через анализ паттернов ответов, а не через прямые вопросы. Данный принцип положен в основу метода сценарных дилемм, используемого в разрабатываемой системе: варианты выбора в каждом сценарии спроектированы таким образом, чтобы выявлять характерные доминирующие эвристики пользователя.

Теория перспектив [10], также разработанная Канеманом и Тверски, демонстрирует глубокую асимметрию восприятия потенциальных выигрышей и потерь. Функция ценности в этой теории имеет S-образную форму: она вогнута для выигрышей (демонстрируя неприятие риска в области прибыли) и выпукла для потерь (демонстрируя стремление к риску ради избегания потерь). Потери психологически весят примерно в 2-2,5 раза больше, чем эквивалентные выигрыши (феномен неприятия потерь).

Эта асимметрия критически важна для измерения **«Контроль ↔ Адаптация»** в нашей модели:

- **Полюс Контроля** выбирают пользователи, стремящиеся к созданию жестких структур для минимизации неопределенности и предотвращения потерь. Для них определенность ценнее потенциальной сверхприбыли.
- **Полюс Адаптации** характерен для тех, кто воспринимает перемены не как угрозу потери, а как пространство возможностей. Они легче отказываются от старых планов («невозвратных затрат») в пользу новых данных.

Концепция индивидуальных стилей принятия решений, детально разработанная С. Скоттом и Р. Брюсом [11], выделяет пять архетипических стилей:

1. **Рациональный** — характеризуется поиск информации и логической оценкой альтернатив.
2. **Интуитивный** — опора на предчувствия и целостные образы ситуации.
3. **Зависимый** — поиск совета и поддержки со стороны окружающих.
4. **Избегающий** — стремление отложить решение до последнего момента.
5. **Спонтанный** — желание принять решение как можно быстрее.

Разрабатываемая система развивает этот подход, вводя 6 непрерывных шкал вместо жесткой классификации. Это позволяет построить уникальный векторный профиль, отражающий тонкие нюансы личности, которые теряются при отнесении к одному из 5 дискретных типов.

Особое внимание уделено **эффекту фрейминга (формулировки)** [12]. Исследования показывают, что решение часто зависит от того, представлен ли выбор в терминах выигрыша или в терминах потери. Сценарии в нашей системе специально сбалансированы: некоторые дилеммы подаются через «позитивный фрейм», другие через «негатив», что позволяет нейтрализовать поверхностное влияние формулировок и выявить истинные, устойчивые предпочтения пользователя в различных контекстах (профессиональном, личном, социальном).

Таким образом, психология принятия решений предоставляет научно обоснованную методологическую базу для разработки цифровой системы самопознания, позволяя выявлять устойчивые паттерны мышления через анализ поведенческих выборов в контексте дилемм.

## 1.2 Модели анализа личности: от Big Five до современных подходов

Для разработки собственной модели анализа предпочтений необходимо критически рассмотреть существующие модели анализа личности, выявить их ограничения и возможности для интеграции в цифровую среду.

**Модель Большой пятёрки (Big Five / OCEAN).** Пятифакторная модель является «золотым стандартом» в современной эмпирической психологии [5]. Она описывает личность через пять широких измерений:

1. **Открытость опыту (Openness)** — любознательность, воображение, готовность к экспериментам.
2. **Добросовестность (Conscientiousness)** — организованность, ответственность, стремление к порядку.
3. **Экстраверсия (Extraversion)** — направленность внимания вовне, потребность в социальном контакте.
4. **Доброжелательность (Agreeableness)** — склонность к сотрудничеству, доверию и альтруизму.
5. **Невротизм (Neuroticism)** — склонность к эмоциональной нестабильности и тревожности.

Достоинством модели является её универсальность и высокая воспроизводимость в кросс-культурных исследованиях. Однако для нашего проекта Big Five имеет ряд недостатков: модель описывает статичные черты, а не динамический процесс принятия решения. Кроме того, она опирается на прямые опросники (например, NEO-PI-R), где респонденты часто завышают баллы по «социально одобряемым» шкалам (добросовестность, доброжелательность).

**Шестифакторная модель HEXACO.** Разработанная М. Эштоном и К. Ли [13], модель HEXACO расширяет Большую пятёрку, добавляя шестой фактор — «Честность-Скромность» (Honesty-Humility).

1. **Честность-Скромность (H):** Оценивает искренность, справедливость, отсутствие жадности и скромность. Люди с высокими показателями по этой шкале не склонны манипулировать другими ради собственной выгоды.
2. **Эмоциональность (E):** Страх, тревога, зависимость, сентиментальность. Высокий уровень указывает на чувствительность к стрессу.
3. **Экстраверсия (X):** Социальная уверенность, социальная смелость, живость. Отражает потребность в социальном взаимодействии.
4. **Доброжелательность (A):** Терпимость, кротость, гибкость, терпение. Характеризует стиль взаимодействия в конфликтных ситуациях.
5. **Добросовестность (C):** Организованность, трудолюбие, перфекционизм, рассудительность. Отражает отношение к работе и планированию.
6. **Открытость опыту (O):** Эстетическая оценка, любознательность, креативность, неконвенциональность. Поиск новых идей и впечатлений.

Внедрение этих измерений позволяет лучше выявлять этические предпочтения пользователей, что отражено в нашем измерении «Смысл ↔ Выгода».

**Типология Майерс-Бриггс (MBTI).** Модель [6], основанная на теории архетипов К. Г. Юнга, классифицирует людей по 16 типам на основе четырех полярных шкал. Несмотря на коммерческий успех, академическое сообщество критикует MBTI за:

- **Низкую валидность:** типы часто не коррелируют с реальным успехом в профессиях.
- **Дискретность:** человек считается либо интровертом, либо экстравертом, хотя большинство людей находятся в середине спектра.
- **Низкую надежность:** до 50% респондентов меняют свой «тип» при замере через несколько недель.
В нашей системе мы отказываемся от деления на типы, сохраняя непрерывность шкал, что позволяет пользователю видеть нюансы своего профиля.

**Модель DISC.** Разработанная У. Марстоном поведенческая модель [15] анализирует реакцию человека на среду. Она удобна для корпоративной среды благодаря своей наглядности, но часто слишком упрощает психологическую картину до четырех квадрантов. Мы заимствовали из DISC идею ориентации на поведение в конкретных ситуациях, но расширили количество переменных.

**Теория самодетерминации и мотивации.** Э. Деси и Р. Райан [7] утверждают, что личность эффективно развивается только при удовлетворении трех нужд: автономии, компетентности и социальной связанности. Это легло в основу измерения «Индивидуализм ↔ Коллективизм»:

- **Автономия** (Индивидуализм) — потребность чувствовать себя инициатором собственных действий.
- **Связанность** (Коллективизм) — потребность в принадлежности к группе и взаимной поддержке.

**Теория потока и мастерства.** М. Чиксентмихайи [16] показал, что счастье и продуктивность связаны с балансом вызова и мастерства. Если вызов слишком мал — возникает скука; если слишком велик — тревога. Наше измерение «Исследователь ↔ Исполнитель» анализирует, к какой стороне этого баланса тяготеет пользователь: ищет ли он постоянного новизны и «потока» в неопределенности или предпочитает достижение мастерства в структурированной, предсказуемой деятельности.

Сравнительный анализ моделей представлен в таблице 1.

**Таблица 1 — Сравнительный анализ академических моделей анализа личности**

| Критерий | Big Five | HEXACO | MBTI | Разрабатываемая модель |
| :--- | :--- | :--- | :--- | :--- |
| Число шкал | 5 | 6 | 4 (дихотомии) | 6 (непрерывные) |
| Концепция | Черты (Trait) | Черты + Этика | Типы (Type) | Стиль принятия решений |
| Мат. модель | Линейная | Линейная | Бинарная | Векторная (нормализ.) |
| Контекст | Общий | Общий | Работа/Жизнь | Сценарный (Life-sim) |
| Приватность | Бумажный/Веб | Веб (Сервер) | Веб (Сервер) | Локальный (Браузер) |

На основе синтеза данных моделей была спроектирована уникальная архитектура анализа, которая сочетает точность Big Five, этическую глубину HEXACO и практическую применимость теории самодетерминации, при этом устраняя проблему «социальной желательности» за счет использования косвенных сценарных выборов.

## 1.3 Цифровые инструменты самопознания: обзор и анализ

Рынок цифровых инструментов самопознания (Digital Self-Knowledge) активно развивается и к 2026 году включает несколько категорий решений, каждая из которых имеет свои технологические и методологические особенности.

**Онлайн-платформы личностного анализа.** Платформы сегмента Mass Market (такие как 16Personalities, Truity, PersonalityPerfect) предлагают прохождение стандартных психологических тестов через дружелюбный веб-интерфейс. Их несомненные достоинства — общедоступность, визуально привлекательный дизайн и мгновенный, легко читаемый результат. Однако, с научной и технологической точек зрения, они имеют ряд фундаментальных ограничений:

- **Эффект Форера (Барнума):** Описания результатов часто формулируются настолько расплывчато и позитивно, что подходят подавляющему числу людей. Пользователи читают описание своего "типа" и соглашаются с ним, принимая за глубокий анализ то, что по сути является набором обобщений.
- **Низкая динамика и дискретность:** Результат выдается в виде жестко зафиксированной карточки (например, "INTJ" или "Логик"), которая статична и не учитывает развитие личности во времени. Дискретный подход отсекает пограничные состояния.
- **Privacy-риски:** Подавляющее большинство систем требуют обязательной регистрации, привязки email или аккаунта в социальных сетях. Данные ответов передаются на сервер для обработки и создания базы для таргетирования рекламы, что в эпоху Больших Данных создает риски деанонимизации психологического профиля.
- **Когнитивная перегрузка и искажения:** Прямые вопросы (например, "Вы часто задумываетесь о смысле жизни?") заставляют пользователя оценивать самого себя, что неизбежно приводит к включению механизмов психологической защиты. Пользователь «подгоняет» ответы под желаемый образ (социальная желательность), а не дает объективную оценку своему реальному поведению.

**Мобильные приложения для мониторинга состояния.** Приложения Reflectly, Daylio, MindDoc ориентированы на ежедневный микро-трекинг (Micro-journaling). Они используют визуальные шкалы настроения и теги активности. Хотя эти инструменты отлично показывают эмоциональные тренды, они редко проводят комплексный анализ стиля принятия решений или когнитивных паттернов. Кроме того, закрытая экосистема мобильных сторов (App Store/Google Play) ограничивает свободу пользователя и часто навязывает подписочную модель.

**Профессиональные и корпоративные инструменты.** Gallup CliftonStrengths, Hogan Assessment, Thomas International предоставляют глубокий анализ, но ориентированы на бизнес-контекст. Их стоимость (от 50 до 300 долларов за отчет) делает их недоступными для массового пользователя (студентов, школьников). Кроме того, они часто требуют участия сертифицированного консультанта для интерпретации результатов.

**AI-ассистенты и LLM-аналитика.** Современный тренд 2024-2026 гг. — использование больших языковых моделей для анализа личности через свободный текст. Это дает высокую индивидуализацию, но страдает от проблемы «галлюцинаций» модели и полного отсутствия прозрачности алгоритма оценки.

Наиболее значимым отличием разрабатываемой системы является **метод сценарных дилемм**. В отличие от прямых вопросов («Вы считаете себя рациональным?»), сценарные дилеммы помещают пользователя в конкретную ситуацию, где выбор действия естественным образом отражает его внутренние паттерны. Это минимизирует эффект «социальной желательности» (желание казаться лучше) и делает процесс тестирования менее утомительным, превращая его в своего рода интерактивное повествование.

Комплексный сравнительный анализ существующих решений представлен в таблице 2.

**Таблица 2 — Сравнительный анализ цифровых инструментов самопознания**

| Критерий | Веб-тесты | Мобильные прил. | AI-ассистенты | Разрабатываемая система |
|---|---|---|---|---|
| Метод анализа | Прямой опрос | Трекинг настроения | Анализ логов/чатов | Сценарные дилеммы |
| Обработка данных | Cloud (Cloud-side) | On-device (App) | Cloud (API) | Local (Browser PWA) |
| Приватность | Низкая | Средняя | Низкая | Максимальная |
| Модель оплаты | Freemium | Subscription | Pay-per-use | Open Source / Free |
| Работа Offline | Нет | Да | Нет | Да (через SW) |
| Визуализация | Статическая | Тренды | Текстовая | Интерактивная (Radar) |

Анализ показывает, что на рынке существует свободная ниша для инструмента, который сочетал бы научную глубину профессиональных тестов, приватность локальных приложений и интерактивность современных веб-технологий. Данный проект направлен на заполнение этой ниши.

## 1.4 Визуализация психологических данных

Визуализация данных играет ключевую роль в системах анализа личности, поскольку позволяет превратить абстрактные цифры и весовые коэффициенты в наглядный образ («цифровой двойник» предпочтений) [8]. Это критически важно для рефлексии: согласно когнитивной психологии, графическая информация обрабатывается мозгом в 60 000 раз быстрее, чем текстовая.

**Радарные (лепестковые) диаграммы** являются основным инструментом в нашем проекте. Их преимущества выходят за рамки простого отображения данных:

1. **Гештальт-восприятие целостности.** Человек воспринимает не набор отдельных точек на осях, а общую площадь и форму фигуры. Уникальная «звезда» профиля становится личным брендом пользователя внутри системы.
2. **Симметрия и баланс.** Радарная диаграмма наглядно показывает перекосы в развитии. Если одна сторона профиля (например, Рациональность) сильно вытянута, а другая (Интуиция) сжата, пользователь мгновенно осознает отсутствие баланса.
3. **Ортогональность.** Размещение измерений на радиальных осях подчеркивает их независимость друг от друга, что соответствует математической модели нашей системы.

**Столбчатые диаграммы** применяются для детализации. В то время как радарная диаграмма показывает «характер», столбчатая диаграмма позволяет увидеть точную разницу между средними значениями и текущим тестом. Мы используем горизонтальные бары для отображения дихотомий (Биполярные шкалы), где центральная линия — это точка равновесия.

**Психофизика цвета.** В системе реализована осознанная цветовая палитра:

- **Star Silver (#e1e8ed)** используется для нейтральных состояний;
- **Cyber Blue (#00c6fb)** и **Deep Blue (#005bea)** для технологичных, рациональных аспектов;
- **Emerald Green (#10b981)** для индикации положительного прогресса или «здорового» диапазона значений.

Для реализации диаграмм выбрана библиотека **Chart.js** [20]. Это мощный движок рендеринга на базе HTML5 Canvas, который обеспечивает:

- **Адаптивность:** графики перестраиваются под размер экрана смартфона или широкоформатного монитора.
- **Интерактивность:** при наведении на точки осей пользователь получает всплывающие подсказки с расшифровкой значений (Data Tooltips).
- **Производительность:** аппаратное ускорение Canvas позволяет плавно анимировать появление графиков даже на бюджетных устройствах.

Особое внимание в визуализации уделено **Accessibility (Доступности)**. Для людей с нарушениями зрения (дальтонизм) мы используем не только цвет, но и символы, а также текстовое дублирование уровней («Высокий», «Средний», «Сбалансированный»), что соответствует стандарту WCAG 2.1.

## 1.5 Прогрессивные веб-приложения как платформа

Прогрессивное веб-приложение (Progressive Web Application, PWA) — это эволюционный шаг в развитии веб-технологий, объединяющий широту охвата веба с мощностью нативных приложений [9]. Для системы самопознания выбор PWA является стратегическим решением, обеспечивающим три ключевых принципа: Надежность, Быстрота, Вовлекаемость.

**Архитектура Service Worker.** Сердцем PWA является Service Worker — событийно-ориентированный прокси-скрипт, работающий в фоновом режиме, отдельно от основной страницы браузера. В нашей системе он реализует следующие уровни логики:

1. **Offline-first кэширование.** При первом запуске скрипт перехватывает сетевые запросы и сохраняет все статические ресурсы (JS-ядро анализатора, JSON-сценарии, CSS-стили) в Cache Storage. Это гарантирует, что приложение откроется даже в лесу или самолете.
2. **Интеллектуальное обновление.** Мы применяем стратегию «Stale-while-revalidate»: приложение мгновенно загружается из кэша, а сервис-воркер в это время тихо проверяет наличие новой версии сценариев на сервере и обновляет кэш «на лету» для следующего запуска.
3. **Обработка фоновых событий.** Хотя текущая версия не требует постоянной синхронизации, архитектура SW позволяет в будущем добавить Push-уведомления с напоминанием о рефлексии.

**Конфиденциальность и Zero-Server подход.** В отличие от классических веб-сайтов, наше PWA работает по принципу «Local First». Все расчеты психологического профиля, нормализация весов и генерация рекомендаций происходят в оперативной памяти браузера пользователя. Для долговременного хранения используется **Web Storage API (localStorage)**. Это дает две критические гарантии:

- **Скорость:** нет задержек на передачу данных по сети (Latency = 0ms).
- **Безопасность:** ваши личные предпочтения никогда не покидают ваше устройство. Никто, включая разработчика системы, не имеет доступа к вашим ответам.

**Установка и пользовательский опыт.** Благодаря **Web App Manifest**, приложение получает иконку на рабочем столе, полноэкранный режим без адресной строки браузера и плавные Splash-скрины. Это стирает грань между веб-сайтом и программой для пользователя. Кроссплатформенность PWA позволяет нам поддерживать Android, iOS, Windows и macOS, используя единую кодовую базу на JavaScript, что значительно снижает стоимость владения и сопровождения проекта.

В таблице 3 приведено расширенное сравнение платформ, подчеркивающее преимущества PWA для систем психометрического анализа.

**Таблица 3 — Комплексное сравнение платформ разработки**

| Критерий | PWA (Наш выбор) | Нативное (React Native) | Классическое SPA |
|---|---|---|---|
| Инсталляция | Без сторов (через браузер) | Через App Store / Google Play | Не поддерживается |
| Стоимость | Низкая (1 код) | Высокая (2-3 платформы) | Низкая |
| Офлайн | Полный доступ | Полный доступ | Только онлайн |
| Скорость запуска | Мгновенно (Cache First) | Высокая | Зависит от интернета |
| Приватность | Local-Only | Зависит от SDK | Server-Side |
| SEO | Индексируется Google/Yandex | Нет | Индексируется |

Таким образом, выбор PWA в качестве платформы позволяет создать по-настоящему свободный, приватный и технологичный инструмент самопознания, доступный любому человеку с любым современным устройством.


---

# 2 ПРОЕКТИРОВАНИЕ СИСТЕМЫ САМОПОЗНАНИЯ

## 2.1 Анализ требований и постановка задачи

На этапе проектирования был проведен глубокий анализ требований, разделенных на функциональные, нефункциональные и бизнес-требования. Это позволило сформировать четкий вектор разработки и минимизировать риски на этапе реализации.

**Функциональные требования (Functional Requirements):**

1. **Сценарный модуль:** проведение тестирования через 12+ интерактивных дилемм. Каждый ответ должен влиять на несколько измерений одновременно (принцип многомерности).
2. **Аналитическое ядро:** расчет профиля в реальном времени. Алгоритм должен учитывать «вес» каждого сценария в зависимости от его сложности.
3. **Когнитивное тестирование:** реализация отдельного модуля для замера времени реакции и когнитивной гибкости.
4. **Визуализационный блок:** построение Radar Chart (общий профиль) и Bar Charts (детальный разбор).
5. **Система интерпретации:** автоматическая генерация психологического портрета на основе пересечения шкал.
6. **Личный кабинет (Локальный):** сохранение истории тестов, возможность переименования и удаления записей.
7. **Экспорт данных:** формирование PDF/HTML отчета для скачивания, содержащего все графики и описания.
8. **Локализация:** полная поддержка интерфейса на казахском (kk), русском (ru) и английском (en) языках.

**Нефункциональные требования (Quality Attributes):**

1. **Privacy-by-Design:** нулевая передача данных на сервер. Все расчеты и хранение — только в браузере.
2. **Offline-Availability:** запуск и работа приложения без доступа к интернету (индикатор статуса в UI).
3. **Performance:** время генерации профиля после завершения теста не должно превышать 150 мс.
4. **Responsiveness:** поддержка мобильных устройств (от iPhone SE) и десктопных мониторов (4K).
5. **Accessibility:** контрастность текста не менее 4.5:1, поддержка скринридеров, навигация с клавиатуры.
6. **PWA-статус:** соответствие чек-листу Google PWA (иконка, манифест, HTTPS-readiness).

**Целевая аудитория и User Personas:**

На основе данного анализа задача была декомпозирована на разработку трех независимых слоев: слой данных (JSON), логический слой (JS-analyzer) и слой представления (PWA-интерфейс).

**Сценарии использования (User Stories):**

- *Как ищущий студент*, я хочу пройти тест быстро и получить понятное описание своего типа личности, чтобы определиться с направлением обучения.
- *Как профессионал*, я хочу сравнить свои результаты с требованиями к различным ролям (Team Lead, Architect), чтобы понять свои зоны роста.
- *Как пользователь, ценящий приватность*, я хочу быть уверенным, что мои ответы не попадут в открытый доступ или к работодателю без моего согласия.

Эти истории легли в основу приоритезации разработки: сначала была реализована кор-логика (ядро), затем визуализация, и в последнюю очередь — дополнительные модули (экспорт, история).

## 2.2 Модель анализа личностных предпочтений по 6 измерениям

### 2.2.1 Выбор и обоснование измерений

Ядром разрабатываемой системы является авторская модель анализа личностных предпочтений, синтезирующая классические подходы (Big Five, DISC) с современными концепциями когнитивных эвристик. Мы используем 6 биполярных измерений, которые образуют многомерное пространство личности.

**Измерение 1: Рациональность ↔ Интуиция (Rationality ↔ Intuition)**
Оценивает доминирующую когнитивную систему обработки информации (System 2 vs System 1 по Канеману) при принятии решений.
*Полюс Рациональности (Высокий балл):* Характеризуется строгой опорой на объективные факты, цифры, исторические данные и дедуктивный метод. Люди с выраженной рациональностью склонны к построению сложных ментальных моделей, составлению таблиц плюсов и минусов, и оценке вероятностей. В профессиональной среде это проявляется у системных аналитиков, инженеров, дата-сайентистов и финансовых директоров. Сильная сторона — минимизация эмоциональных ошибок; слабая сторона — риск "паралича анализа" (analysis paralysis), когда решение откладывается из-за нехватки 100% данных.
*Полюс Интуиции (Низкий балл):* Опирается на подсознательное распознавание паттернов, "чувство рынка" (gut feeling) и богатый ассоциативный опыт. Такие пользователи способны принимать верные решения в условиях тотальной неопределенности, когда логический расчет невозможен. Этот паттерн часто встречается у успешных предпринимателей-визионеров, креативных директоров и антикризисных менеджеров. Сильная сторона — высочайшая скорость реакции; слабая сторона — подверженность когнитивным искажениям.

**Измерение 2: Контроль ↔ Адаптация (Control ↔ Adaptation)**
Отражает эмоциональную и поведенческую стратегию управления рисками в нестабильной среде.
*Полюс Контроля (Высокий балл):* Выражается в потребности структурировать хаос через жесткое планирование (на 5-10 шагов вперед), создание подробных регламентов, чек-листов и систем защиты от сбоев. Главная психологическая цель — максимальное снижение базовой тревожности через предсказуемость. Такие люди великолепно справляются с управлением сложными инфраструктурными проектами.
*Полюс Адаптации (Низкий балл):* Характеризуется высокой толерантностью к неопределенности. Пользователь воспринимает резкие изменения планов не как угрозу безопасности, а как пространство новых возможностей. Для них характерно умение "ловить волну", применять Agile-подходы в мышлении и легко списывать "невозвратные затраты". Это идеальные специалисты для работы в стартапах на ранних стадиях (early stage).

**Измерение 3: Стратегия ↔ Тактика (Strategic ↔ Tactical)**
Оценивает масштаб временного горизонта планирования и ширину фокуса внимания пользователя.
*Полюс Стратегии (Высокий балл):* Фокус на макро-целях с горизонтом от года до десятилетий. Стратегическое мышление означает видение системы целиком ("helicopter view"), понимание взаимосвязей между разрозненными элементами. Человек с высоким баллом готов пожертвовать краткосрочной тактической выгодой (или даже проиграть локальную битву) ради выигрыша в долгосрочной перспективе. В работе это проявляется как архитектурное мышление и проектирование долгосрочных траекторий развития продукта.
*Полюс Тактики (Низкий балл):* Фокус внимания жестко сфокусирован на "здесь и сейчас". Высокая оперативность, внимание к техническим деталям реализации и непревзойденное умение "тушить пожары". Тактик не отвлекается на абстрактные размышления о будущем десятилетии, он мастерски решает текущие проблемы и обеспечивает безупречное функционирование процессов сегодня. Это "инженерное" мышление, ориентированное на Execution (моментальное исполнение).

**Измерение 4: Исследование ↔ Исполнение (Explorer ↔ Executer)**
Отражает отношение пользователя к новизне, экспериментам, творчеству и рутинным, повторяющимся процессам.
*Полюс Исследователя (Высокий балл):* Непрерывная жажда новизны ("novelty seeking"). Такой пользователь всегда ищет новые, нестандартные методы работы, даже если старые функционируют безупречно. Повторение одних и тех же действий быстро вызывает глубокую скуку и падение мотивации. Исследователь — это драйвер радикальных (disruptive) инноваций в команде, он видит потенциал там, где другие видят лишь необоснованный риск и нарушение правил.
*Полюс Исполнителя (Низкий балл):* Склонность к доведению до совершенства уже известных и проверенных процессов. Характеризуется высочайшей дисциплиной, надежностью и методичностью. Для исполнителя понятные рамки и правила — это не ограничение свободы, а комфортная зона для оттачивания мастерства. Исполнитель отвечает за масштабирование бизнесов и гарантирует предсказуемый стандарт качества в долгосрочной перспективе (операционная эффективность).

**Измерение 5: Индивидуализм ↔ Коллективизм (Individualism ↔ Collectivism)**
Оценивает социальный вектор достижений и модель взаимодействия с социумом при решении задач.
*Полюс Индивидуализма (Высокий балл):* Ориентация на личную ответственность, полную автономность и конкуренцию как главный драйвер роста. Индивидуалист не любит делить ответственность с группой, предпочитая, чтобы результат напрямую зависел только от его личных усилий и компетенций. Такие люди расцветают в ролях соло-основателей, независимых консультантов или экспертов узкого профиля.
*Полюс Коллективизма (Низкий балл):* Глубокая вера в синергию ("один в поле не воин"). Успех воспринимается исключительно как результат командной работы. Пользователь с этим преобладающим полюсом ценит взаимную поддержку, консенсус и гармоничный психологический климат внутри коллектива выше личных амбиций. Это незаменимые тимлиды, HR-директора и scrum-мастера.

**Измерение 6: Смысл ↔ Утилитарность (Meaning ↔ Utilitarianism)**
Оценивает парадигму внутренней мотивации и ценностные ориентиры деятельности.
*Полюс Смысла (Высокий балл):* Приоритезация глобальной миссии, этических ценностей и позитивного влияния на общество (Impact). Для такого человека финансовая компенсация или статус вторичны по отношению к идее. Основной вопрос, который задает себе пользователь: «Зачем я это делаю? Оставит ли это след?». Характерно для социальных предпринимателей, ученых, основателей non-profit организаций и визионеров.
*Полюс Утилитарности (Низкий балл):* Жесткий прагматизм, фокус на КПД, ROI (окупаемость инвестиций) и конкретный материальный или измеримый результат. Главные вопросы: «Как это работает?» и «Какую конкретную выгоду это принесет здесь и сейчас?». Это ориентация на коммерческий успех, бизнес-показатели, оптимизацию ресурсов. Отличные предприниматели-прагматики и кризис-менеджеры.

Модифицированная 6D-модель позволяет получить свыше 64 устойчивых базовых комбинаций профилей. В отличие от типологий вроде MBTI, наша математическая модель не округляет результаты: пользователь может быть на 70% Стратегом и на 30% Тактиком, что обеспечивает беспрецедентную точность описания. Каждое измерение матрично независимо, что достигается через ортогональное проектирование весов в каждом сценарии.

### Математическая модель скоринга и нормализации

Для обеспечения объективности выводов в системе используется двухстадийная математическая обработка данных.

**Стадия 1: Векторная сумма весов**
Каждый вариант ответа $O_i$ в сценарии $S_j$ имеет вектор весов $W_{ij} = [w_1, w_2, ..., w_6]$, где $w_k \in [-1, 1]$.
Итоговый сырой балл по измерению $k$ рассчитывается как:
$$R_k = \sum_{j=1}^{N} w_{kj}$$
где $N$ — количество пройденных сценариев.

**Стадия 2: Динамическая нормализация**
Для приведения баллов к стандартному диапазону $[-1, 1]$ используется функция нормализации с учетом доверительного интервала:
$$N_k = \tanh\left(\frac{R_k}{\sigma \cdot \sqrt{N}}\right)$$
Применение гиперболического тангенса позволяет плавно «сжимать» экстремальные значения, что делает профиль более устойчивым к случайным выбросам в ответах.

**Стадия 3: Расчет архетипа**
Архетип определяется как вектор $A$, минимизирующий косинусное расстояние до вектора пользователя $U$:
$$\text{Archetype} = \arg\max_{A_i} \frac{U \cdot A_i}{\|U\| \|A_i\|}$$
Это гарантирует, что пользователю будет назначен тот психологический портрет, который наиболее близок ему по «направленности» характера, а не просто по сумме баллов.

### 2.2.2 Алгоритм нормализации оценок

Для количественной оценки предпочтений пользователя разработан алгоритм нормализации, включающий следующие этапы.

**Этап 1. Определение весов.** Для каждого варианта выбора в каждом сценарии экспертно определены веса по всем 6 измерениям. Веса принимают значения в диапазоне от -1.0 до +1.0:

- положительные значения соответствуют первому полюсу (например, рациональность);
- отрицательные значения — второму полюсу (например, интуиция);
- нулевое значение означает нейтральность по данному измерению.

**Этап 2. Регистрация выбора.** При каждом выборе пользователя фиксируются веса выбранного варианта. Для каждого измерения d накапливается сумма весов:

    S(d) = Σ w(d, i),    i = 1, 2, ..., N                          (1)

где w(d, i) — вес выбора i-го сценария по измерению d, N — общее число пройденных сценариев.

**Этап 3. Нормализация.** Суммарный балл делится на количество пройденных сценариев:

    Norm(d) = S(d) / N                                               (2)

**Этап 4. Ограничение диапазона.** Нормализованное значение ограничивается интервалом [-1, 1]:

    Score(d) = clamp(Norm(d), -1, 1)                                 (3)

где clamp(x, a, b) = max(a, min(b, x)).

**Этап 5. Перевод в процентное представление** для визуализации:

    P(d) = (Score(d) + 1) × 50                                      (4)

Результат P(d) находится в диапазоне от 0% до 100%, где 50% соответствует нейтральному (сбалансированному) положению.

**Этап 6. Определение уровня.** На основе значения Score(d) определяется качественный уровень:

    |Score(d)| > 0.5   →  высокий (выраженная склонность)
    |Score(d)| > 0.2   →  средний (умеренная склонность)
    |Score(d)| ≤ 0.2   →  сбалансированный (нет выраженной склонности)

Данный алгоритм обеспечивает:

- нормализованные значения, не зависящие от количества пройденных сценариев;
- устойчивость к единичным «нетипичным» выборам (усреднение сглаживает случайные отклонения);
- корректное представление как для визуализации, так и для дальнейшей аналитики.

### 2.2.3 Система категоризации и рекомендаций

На основе комбинации значений по 6 измерениям система определяет принадлежность пользователя к одной или нескольким категориям направлений развития. Для каждой категории задан вектор весов, определяющий, какие измерения вносят наибольший вклад.

Процент соответствия пользователя категории c вычисляется по формуле:

    Match(c) = Σ (W(c, d) × Score(d)) / Σ |W(c, d)|,   d = 1..6    (5)

где W(c, d) — вес измерения d для категории c, Score(d) — нормализованная оценка пользователя по измерению d.

Основные категории и их весовые конфигурации:

а) **Исследование и Наука** — высокие веса rationality (+0.8) и explorer (+0.9);

б) **Творчество и Инновации** — высокие веса explorer (+0.8) и individualism (+0.7);

в) **Управление и Лидерство** — высокие веса strategic (+0.9) и control (+0.7);

г) **Социальная сфера** — высокие веса collectivism (+0.8, т.е. отрицательный individualism) и meaning (+0.7).

Система также определяет **архетип личности** — обобщённый профиль, наиболее соответствующий комбинации значений пользователя. Каждый архетип описывается названием, описанием, характерными чертами, цветом, иконкой и процентом соответствия. Выбирается архетип с максимальным Match(c).

### Глоссарий психологических архетипов

Для интерпретации результатов в системе реализовано 12 базовых архетипов, представляющих собой устойчивые комбинации векторов 6-мерной модели.

**1. Системный Инноватор (System Innovator)**

- *Профиль:* Высокая рациональность, стратегия, исследование.
- *Описание:* Видит мир через призму сложных взаимосвязанных систем. Идеален для ролей Enterprise Architect и CTO.
- *Сильные стороны:* Способность предсказывать долгосрочные последствия технических решений.

**2. Гибкий Тактик (Agile Tactician)**

- *Профиль:* Высокая адаптация, тактика, исполнение.
- *Описание:* Мастер решения проблем «здесь и сейчас». Незаменим в кризисных ситуациях и при быстром прототипировании.
- *Сильные стороны:* Мгновенная реакция на изменения, практическая смекалка.

**3. Идейный Лидер (Inspirational Leader)**

- *Профиль:* Интуиция, коллективизм, смысл.
- *Описание:* Мотивирует команду через общие ценности и миссию. Ориентирован на рост людей и гармонию в коллективе.
- *Сильные стороны:* Высокий эмоциональный интеллект, талант убеждения.

**4. Прагматичный Контролер (Pragmatic Controller)**

- *Профиль:* Контроль, утилитарность, индивидуализм.
- *Описание:* Максимально ориентирован на результат и эффективность. Ценит порядок, дисциплину и четкие KPI.
- *Сильные стороны:* Высокая надежность, доведение задач до конца.



## 2.3 Проектирование структуры данных сценариев

Сценарии в нашей системе являются не просто набором вопросов, а сложными объектами данных, инкапсулирующими психологическую логику и многоязычный контент. Использование формата JSON оправдано его легкостью, человекочитаемостью и нативной поддержкой в JavaScript.

**Углубленная структура сценария:**

- `metadata`: Содержит служебную информацию. Поле `primaryDimensions` позволяет анализатору «подсвечивать» наиболее важные измерения для данного сценария, а `difficulty` используется для весовой коррекции (чем сложнее дилемма, тем выше вес выбора).
- `context`: Развернутое описание ситуации, создающее «эффект погружения» (immersion). Это критично для обхода психологических защит пользователя.
- `options (A-D)`: Каждый вариант содержит не только текст, но и **Вектор Весов**. Например: `"weights": { "rationality": 0.8, "control": -0.4 }`. Обратите внимание, что сумма весов по одному измерению внутри одного сценария не обязательно равна нулю — это позволяет реализовывать асимметричные дилеммы.

**Стратегия локализации:**
Мы выбрали объектно-ориентированный подход к i18n внутри JSON. Вместо разделения сценариев на разные файлы (`scenarios_ru.json`, `scenarios_en.json`), мы храним переводы внутри каждого ключа: `"title": { "kk": "...", "ru": "...", "en": "..." }`.
*Преимущества этого подхода:*

1. **Целостность данных:** Веса и структура одинаковы для всех языков, исключена рассинхронизация.
2. **Атомарность загрузки:** Приложение загружает один файл и мгновенно переключает язык без сетевых запросов.
3. **Легкость расширения:** Добавление нового языка (например, французского) требует лишь добавления ключа `fr` в существующие объекты.

В текущей версии реализовано **15 эталонных сценариев**, разделенных по психометрическим кластерам:

1. **Профессиональные:** «Крах проекта», «Предложение конкурента», «Выбор технологии».
2. **Личностные:** «Творческий кризис», «Неожиданный выигрыш», «Смена города».
3. **Социальные:** «Конфликт интересов», «Ошибка коллеги», «Общественное признание».
4. **Когнитивные:** «Инновация vs Традиция», «Скорость vs Качество».

## 2.4 Архитектура приложения

Приложение спроектировано как **Single Page Application (SPA)** на базе ванильного JavaScript (ES6+), что обеспечивает максимальную производительность и отсутствие «мертвого веса» фреймворков.

**Уровни абстракции:**

1. **Storage Layer (localStorage):** Реализует паттерн «Repository». Модуль `StorageManager` абстрагирует работу с браузерным хранилищем, обеспечивая сериализацию/десериализацию JSON-профилей и проверку целостности данных.
2. **Business Logic Layer (Analyzer):** «Мозг» системы. Модуль `PersonalityAnalyzer` выполняет векторное сложение баллов и нормализацию по Гауссу. Здесь же реализован `RecommendationEngine`, который сопоставляет вектор пользователя с векторами эталонных профессий (методом косинусного сходства).
3. **Presentation Layer (DOM Controller):** Модуль `UIController` работает по принципу «State-to-UI». Каждое изменение состояния (например, `currentState = 'RESULTS'`) вызывает цепочку рендеринга соответствующих блоков.

**Жизненный цикл Service Worker:**
Для обеспечения высокой надежности (Resilience) реализован SW со следующими фазами:

- **Install:** Формирование статического кэша (App Shell).
- **Activate:** Удаление устаревших версий кэша.
- **Fetch:** Перехват запросов (Strategy: Cache First for static, Network First for scenarios).

**Безопасность и Валидация:**
Несмотря на отсутствие сервера, в приложении реализована валидация входных данных. Перед сохранением в `localStorage`, объект профиля проверяется на соответствие схеме (Schema Validation), что предотвращает затирание данных при обновлении версии приложения.

## 2.5 Проектирование пользовательского интерфейса

Проектирование интерфейса системы велось с учётом принципов удобства (usability) и доступности (accessibility) [24]. Основная цель — создать среду, где пользователь может сосредоточиться на принятии решений без лишнего визуального шума.

**Таблица 4 — Матрица соответствия сценариев и измеряемых параметров**

| ID сценария | Название | Рац/Инт | Конт/Адап | Страт/Такт | Иссл/Исп | Инд/Колл | Смысл/Утил |
| :--- | :--- | :---: | :---: | :---: | :---: | :---: | :---: |
| S-01 | Кризис в проекте | X | X | | | | |
| S-02 | Выбор технологии | X | | | X | | |
| S-03 | Командный конфликт | | | | | X | X |
| S-04 | Внезапный шанс | | X | X | | | |
| S-05 | Рутина vs Хаос | | X | | X | | |
| S-06 | Этика vs Прибыль | | | | | | X |
| S-07 | Глобальное видение | | | X | | | X |
| S-08 | Поиск наставника | | | | | X | |
| S-09 | Архитектура системы | X | | X | | | |
| S-10 | Финал проекта | | | | X | X | |

*Примечание: "X" обозначает основное измерение, на которое влияет данный сценарий.*

Каждый сценарий содержит 4 варианта выбора (A, B, C, D). Такая структура позволяет собирать до 40 точек данных за один краткий тест, обеспечивая высокую статистическую достоверность результата.

**Экраны приложения:**
а) **Вводный экран (Intro)** — приветствие с анимированным текстом.
б) **Экран авторизации (Auth)** — поддержка Google Identity Services.
в) **Экран сценария (Testing)** — карточный интерфейс с плавными переходами.
г) **Экран результатов (Results)** — радарная диаграмма и блок архетипов.
д) **Страница профиля (Profile)** — таймлайн истории тестирований.

Инструментарий навигации вынесен в Sidebar, что позволяет освободить 100% полезной площади экрана под контент тестирования. Применяется фиксированная сетка (Layout Grid), адаптирующаяся под смартфоны, планшеты и десктопы.


---

# 3 РЕАЛИЗАЦИЯ СИСТЕМЫ САМОПОЗНАНИЯ

## 3.1 Выбор технологий и обоснование

Решение об использовании стека **Vanilla JavaScript + CSS3 + HTML5** вместо современных реактивных фреймворков (React, Vue, Angular) было принято на основе глубокого архитектурного анализа.

**Таблица 5 — Сравнительный анализ инструментов фронтенд-разработки**

| Критерий | Vanilla JavaScript | React.js | Vue.js | Обоснование для проекта |
| :--- | :---: | :---: | :---: | :--- |
| Размер бандла | < 100 КБ | > 200 КБ | > 150 КБ | Критично для мгновенного FCP |
| Виртуальный DOM | Нет | Да | Да | Меньше накладных расходов на CPU |
| Изучаемость | Высокая | Средняя | Высокая | Простота поддержки кода |
| Lifetime | Гарантированно | Зависит от Meta | Зависит от сообщества | Долгосрочная актуальность |

**Преимущества выбранного подхода:**

1. **Zero Runtime Overhead:** Отсутствие библиотеки-посредника позволяет браузеру начать выполнение кода сразу после загрузки. Это дает выигрыш в 2-3 секунды времени до готовности приложения (TTI).
2. **Безопасность:** Использование ванильного JS минимизирует количество сторонних зависимостей, что снижает риск атак через цепочку поставок.
3. **Экологичность:** Меньшее количество циклов процессора для рендеринга снижает энергопотребление устройства.

Для визуализации данных выбрана библиотека **Chart.js**. Она обеспечивает оптимальный баланс между весом и мощностью рендеринга через HTML5 Canvas.

**Архитектура "App Shell" и Offline Capability.** Приложение следует паттерну App Shell. Минимальный набор ресурсов кэшируется Service Worker-ом при первом входе. Это гарантирует, что система самопознания всегда доступна пользователю.

Структура файлов проекта представлена в таблице 6.

**Таблица 6 — Структура файлов проекта**

| Каталог / Файл | Назначение | Строк кода |
| :--- | :--- | :--- |
| index.html | Главная страница приложения | ~300 |
| profile.html | Страница профиля пользователя | ~200 |
| about.html | Страница «О системе» | ~250 |
| js/app.js | Главный модуль приложения | ~800 |
| js/ui/ui-controller.js | Контроллер пользовательского интерфейса | ~800 |
| js/ui/views/ResultsView.js | Компонент отображения результатов | ~356 |
| js/ui/profile-extensions.js | Расширения страницы профиля | ~800 |
| js/analysis/analysis.js | Модуль анализа личности | ~400 |
| js/analysis/comparative-analysis.js | Сравнительный анализ с ролями | ~300 |
| js/analysis/celebrity-service.js | Сравнение со знаменитостями | ~250 |
| js/analysis/evolution-tracker.js | Отслеживание эволюции профиля | ~200 |
| js/vis/visualization.js | Модуль визуализации | ~1600 |
| js/services/storage-manager.js | Управление хранилищем данных | ~200 |
| js/services/i18n.js | Модуль локализации | ~150 |
| js/services/auth-manager.js | Управление аутентификацией | ~200 |
| js/services/report-generator.js | Генерация HTML-отчётв | ~292 |
| js/data/scenarios-data.js | Данные сценариев | ~1300 |
| css/*.css | Стили оформления (6 файлов) | ~2000 |
| i18n/*.json | Файлы локализации (3 файла) | ~600 |
| sw.js | Service Worker | ~100 |
| manifest.json | Манифест PWA | ~30 |
| **Итого** | **~42 файла** | **~11000** |

## 3.2 Реализация архитектурных модулей

### 3.2.1 Управление состоянием и навигацией

Модуль `app.js` реализует паттерн «Одиночка» (Singleton) и управляет глобальным состоянием. Навигация в SPA реализована через `hashchange` и скрытие/отображение секций DOM, что исключает перезагрузку страницы.

### 3.2.2 Модуль сценарного тестирования и расширенные типы данных

Сценарный движок является наиболее динамичной частью системы. Он реализует логику «древовидного» прохождения, когда выбор в одном сценарии может теоретически влиять на доступность последующих (хотя в текущей версии используется линейная последовательность для обеспечения статистической сопоставимости).

Ключевой фрагмент обработки выбора с поддержкой многомерности и нормализации в реальном времени:

```javascript
/**
 * Регистрирует выбор пользователя и выполняет векторное обновление
 * @param {string} scenarioId - уникальный ID сценария
 * @param {number} optionIndex - индекс выбора пользователя (0-3)
 */
registerChoice(scenarioId, optionIndex) {
    const scenario = this.data.find(s => s.id === scenarioId);
    if (!scenario) {
        console.error(`Missing data for scenario: ${scenarioId}`);
        return;
    }

    const selectedOption = scenario.options[optionIndex];
    
    // Векторная сумма весов (6 независимых измерений)
    // Веса могут быть как положительными, так и отрицательными
    for (const [dimKey, weight] of Object.entries(selectedOption.weights)) {
        if (typeof this.scores[dimKey] !== 'undefined') {
            this.scores[dimKey] += weight;
        }
    }

    // Сохранение в стек истории для возможности отмены действия (Undo)
    this.history.push({
        scenarioId,
        choice: optionIndex,
        weights: selectedOption.weights,
        snapshot: { ...this.scores }
    });

    this.checkCompletion();
}
```

**Особенности реализации UI Controller:**
Для снижения нагрузки на процессор при рендеринге сложных радарных диаграмм используется метод `Chart.update('none')`, который отключает анимацию при быстрой смене кадров. Это критично для бюджетных смартфонов, где избыточная анимация может приводить к «зависанию» интерфейса.

### 3.2.3 Модуль локального хранения (StorageManager)

Этот модуль является критическим для Privacy-by-Design. Он инкапсулирует работу с `localStorage` и обеспечивает атомарные операции сохранения.

```javascript
class StorageManager {
    static SAVE_KEY = 'autism_pwa_data_v2';

    static saveProfile(profileData) {
        try {
            const data = JSON.parse(localStorage.getItem(this.SAVE_KEY) || '{}');
            data.profiles = data.profiles || [];
            data.profiles.push({
                ...profileData,
                savedAt: new Date().toISOString()
            });
            localStorage.setItem(this.SAVE_KEY, JSON.stringify(data));
        } catch (e) {
            console.error('Failed to save to localStorage', e);
            return false;
        }
    }
}
```

Такой подход гарантирует, что профили пользователей не покидают устройство, что соответствует современным стандартам безопасности данных (GDPR).

**Расширенный тест** включает дополнительные форматы вопросов:

а) **Шкальные вопросы (scale)** — пользователь устанавливает значение на ползунке (range input). Обработчик считывает выбранное значение и нормализует его к диапазону весов.

б) **Открытые вопросы (open)** — текстовый ответ в свободной форме через textarea. Анализ текста выполняется по ключевым словам для определения тональности ответа.

в) **Ситуационные вопросы (situational)** — многошаговые сценарии, в которых каждый шаг предлагает новый выбор, а итоговая оценка складывается из всех шагов.

**Когнитивный тест** оценивает когнитивный стиль мышления через специализированные задания, включающие задачи на логику, ассоциативное мышление и анализ паттернов. Результаты когнитивного теста сохраняются отдельно и отображаются на странице профиля.

## 3.3 Реализация модуля анализа личности и математического ядра

Модуль анализа, представленный классом `PersonalityAnalyzer`, является «вычислительным центром» системы. В его задачи входит не только агрегация сырых баллов, но и их сложная статистическая интерпретация.

**Алгоритм интерпретации профиля и расчет архетипов:**

1. **Многофакторная нормализация:** Каждый вектор баллов делится на количество пройденных сценариев и приводится к диапазону `[-1, 1]` с использованием функции нелинейного сглаживания. Это позволяет избежать «краевых эффектов», когда экстремальные выборы могут исказить общую картину.
2. **Кластеризация и выделение доминант:** Выделение значимых отклонений. Значения в диапазоне `[-0.2, 0.2]` считаются «нейтральными» или «сбалансированными». Значения выше `|0.5|` помечаются как доминантные черты, которые и формируют «лицо» архетипа.
3. **Template Mapping и Генерация описаний:** Система использует хэш-таблицу психологических дескрипторов. Мы применили технологию «Shadow DOM» для мгновенного обновления текстовых блоков без перерисовки всей страницы.

**Сравнение с эталонными ролями (ComparativeAnalysis):**
Для расчета соответствия роли (Match Score) мы используем формулу евклидова расстояния в 6-мерном пространстве, адаптированную для оценки сходства направлений векторов:

```javascript
/**
 * Рассчитывает процент соответствия пользователя заданной роли
 * @param {Object} userProfile - нормализованные баллы пользователя (-1..1)
 * @param {Object} roleVector - эталонные баллы роли
 */
calculateMatch(userProfile, roleVector) {
    let sumSquares = 0;
    let dimensionsCount = 0;

    for (const dim in roleVector) {
        if (userProfile.hasOwnProperty(dim)) {
            // Квадрат разности координат
            sumSquares += Math.pow(userProfile[dim] - roleVector[dim], 2);
            dimensionsCount++;
        }
    }
    
    // Евклидово расстояние
    const distance = Math.sqrt(sumSquares);
    const maxPossibleDistance = Math.sqrt(dimensionsCount * 4); // т.к. макс разница между -1 и 1 это 2
    
    // Приведение к процентам (100% - идеальное совпадение)
    const matchPercent = Math.max(0, 100 * (1 - (distance / maxPossibleDistance)));
    return Math.round(matchPercent);
}
```

Этот метод позволяет пользователю увидеть не просто бинарный результат, а точный процент соответствия (например, «Ваше соответствие роли Архитектора — 82%»), что значительно повышает доверие к системе и дает почву для осознанного профессионального роста.

## 3.4 Реализация модуля визуализации (ResultsVisualizer)

Визуализация построена на базе библиотеки **Chart.js 4.x**. Выбор этой библиотеки обусловлен ее высокой производительностью при работе с HTML5 Canvas, что критически важно для плавного рендеринга на мобильных устройствах. Мы полностью кастомизировали стандартное поведение графиков для создания уникального пользовательского опыта.

**Гексагональная геометрия профиля.** Выбор 6 измерений (осей) имеет не только психологическое, но и геометрическое обоснование. На радарной диаграмме шесть осей образуют гексагон (шестиугольник) — фигуру, которая оптимальна для гештальт-восприятия. Человеческий глаз мгновенно считывает симметрию или перекосы в гексагональной сетке. Если бы измерений было 5 (как в Big Five), диаграмма выглядела бы как скошенный пятиугольник, что подсознательно вызывает ощущение нестабильности. Если измерений 8 или более — фигура стремится к кругу, теряя выразительную «остроту» углов. Таким образом, 6 осей обеспечивают идеальный баланс между детализацией данных и ясностью визуального образа.

**Ключевые настройки графиков и оптимизация производительности:**

- **Dynamic Color Injection:** Цвета осей и градиентной заливки меняются в зависимости от доминирующего архетипа пользователя. Например, для архетипа «Искатель» (высокий показатель Исследования) применяется активный оранжево-красный градиент, а для «Аналитика» (высокая Рациональность) — строгий холодно-синий. Это создает эмоционально-ассоциативную связь между формой профиля и его цветом.
- **Оптимизация рендеринга (Canvas Backend):** Для исключения «заиканий» интерфейса на слабых процессорах смартфонов мы отключили глобальные анимации библиотеки при перерисовке `Chart.update('none')`. Плавность появления графика при первоначальной загрузке обеспечивается CSS-транзициями контейнера (fade-in), а не вычислением промежуточных кадров в JavaScript.
- **Интерактивные Tooltips:** При наведении мыши (или тапе на мобильном) на точку оси графика, дефолтный tooltip перехватывается нашим обработчиком. Отображается не только числовое значение (например, 75%), но и краткое человекочитаемое пояснение психологического смысла конкретной координаты (например, "Выраженный стратег: склонность к макро-планированию").
- **Кросс-девайсная адаптивность (Responsiveness):** Использование атрибута `maintainAspectRatio: false` внутри родительского контейнера с позиционированием `position: relative` позволяет векторному графику корректно растягиваться и сжиматься на смартфонах с нестандартным соотношением сторон (например, 21:9 ultra-wide экраны). Динамический ресайз пересчитывает координаты каждого узла без деградации качества (пикселизации).

**Код инициализации с кастомными плагинами:**

```javascript
this.chart = new Chart(ctx, {
    type: 'radar',
    data: data,
    options: {
        plugins: {
            legend: { display: false },
            tooltip: {
                callbacks: {
                    label: (context) => `Уровень: ${context.raw}%`
                }
            }
        },
        scales: {
            r: {
                min: 0,
                max: 100,
                ticks: { stepSize: 20, display: false },
                pointLabels: { font: { size: 14, weight: 'bold' } }
            }
        }
    }
});
```

Такая реализация обеспечивает не только информативность, но и эстетическую привлекательность интерфейса, что повышает вовлеченность пользователя (User Engagement).

## 3.5 Реализация дополнительных модулей и сервисов

### 3.5.1 Модуль интернационализации (i18n.js)

Для поддержки трех языков была реализована система динамической подстановки ключей. В отличие от стандартных решений, мы оптимизировали процесс для PWA:

- **Fallback Mechanism:** Если ключ отсутствует в выбранном языке, система автоматически подтягивает его из русского словаря.
- **Pluralization:** Реализована базовая логика склонения числительных (например, «1 сценарий», «2 сценария», «5 сценариев»), что важно для качественного UX на русском и казахском языках.

### 3.5.2 Жизненный цикл Service Worker и Offline-логика

Превращение обычного веб-сайта в полноценное PWA-приложение, независимое от состояния сети (Offline-First), технически обеспечивается через использование Service Worker (`sw.js`). Этот скрипт выполняется браузером в фоновом режиме, выступая в роли сетевого прокси-сервера. Мы реализовали классический жизненный цикл воркера с продвинутой стратегией кэширования **Stale-While-Revalidate**.

Жизненный цикл Service Worker в нашей системе состоит из трех критических фаз:

1. **Фаза Install (Установка).** Транслируется событие `install`. На этом этапе воркер открывает специфицированный объект `caches` (например, `CACHE_NAME = 'self-knowledge-v1.2'`) и осуществляет Pre-caching (предварительную загрузку). Скрипт принудительно скачивает статическое "ядро" приложения (App Shell): `index.html`, бандлы стилей (`v-app.css`), скомпилированные JS-скрипты, JSON-файлы локализации и картинки. До завершения скачивания всех ресурсов (`event.waitUntil(...)`) воркер не переходит в активное состояние. Итоговая сборка занимает в кэше около 1.5 МБ.
2. **Фаза Activate (Активация).** Срабатывает событие `activate`. На этом этапе воркер принимает управление приложением на себя. Критически важный момент — инвалидация старого кэша. Скрипт перебирает все сохраненные версии кэшей и удаляет те, чьи имена не совпадают с текущим `CACHE_NAME`. Это защищает пользователей от "застревания" на старой версии (Stale Cache) после того, как мы выкатываем обновление с новыми психологическими сценариями.
3. **Фаза Fetch (Перехват запросов).** Это операционная фаза воркера. Каждый сетевой запрос от браузера (скачивание картинки, запрос перевода) перехватывается обработчиком события `fetch`. Стратегия *Stale-While-Revalidate* работает следующим образом: воркер **мгновенно** отдает запрошенный ресурс из локального кэша (скорость загрузки порядка 5-10 мс), благодаря чему пользователь сразу видит интерфейс. Одновременно с этим (While-Revalidate), скрипт фоном отправляет реальный HTTP-запрос к серверу. Если на сервере файл обновился, воркер тихо скачивает его и кладет в кэш. При следующем заходе пользователь получит уже обновленную версию.

Такая архитектура гарантирует 100% доступность приложения даже при отсутствии интернет-соединения (Airplane mode) или при нестабильном 3G/EDGE покрытии, что является огромным конкурентным преимуществом по сравнению с классическими SPA (Single Page Applications) или серверными монолитами. В будущих версиях данный механизм позволит безболезненно внедрить Background Sync для фоновой выгрузки аналитики при появлении устойчивой связи.

### 3.5.3 Генератор отчетов (ReportGenerator)

Экспорт данных реализован через формирование динамического Blob-объекта. Мы генерируем полноценный HTML-файл, в который встраиваем:

- Стили в теге `<style>` (для независимости файла).
- Растровую копию радарной диаграммы (через `canvas.toDataURL()`), чтобы график отображался даже без библиотек.
- Текстовую интерпретацию профиля.

### 3.5.4 Модуль аутентификации и персональных настроек (auth-manager.js)

Хотя система работает по принципу «Local First», для повышения удобства реализована интеграция с **Google Identity Services**. Это позволяет пользователю:

- Опционально синхронизировать свои профили между устройствами.
- Защитить доступ к истории тестирования на общих компьютерах.
Вся логика аутентификации работает в изолированном песочном режиме (IFrame Sandbox), что исключает доступ сторонних скриптов к токенам пользователя.

### 3.5.5 Модуль монетизации (Donation System)

Для обеспечения финансовой устойчивости и оценки коммерческого потенциала проекта реализована глобальная система микротранзакций (донатов). Модель монетизации построена на принципах добровольной поддержки (Pay-What-You-Want) с использованием платформы BuyMeACoffee.
Интеграция выполнена через кастомный UI-компонент, гармонично вписанный в дизайн-систему приложения (Glassmorphism). Алгоритм отображения запроса на поддержку оптимизирован для снижения показателя отказов (Bounce Rate): уведомление появляется с заданным интервалом (каждые 3 минуты) и автоматически скрывается через 15 секунд, что обеспечивает баланс между бизнес-конверсией и комфортом пользователя (UX).

### 3.5.6 Система обратной связи и Customer Development

Для непрерывного улучшения продукта на основе реальных данных в систему интегрирован модуль обратной связи (Feedback Form), выступающий в роли инструмента Customer Development (CustDev).
Форма обратной связи, подключенная через бессерверную архитектуру (Formspree API), позволяет собирать инсайты от аудитории, валидировать продуктовые гипотезы и оперативно формировать бэклог задач. Раздел контактов спроектирован с учетом коммуникаций, предоставляя удобные точки входа для пользователей, B2B-клиентов и инвесторов.

---

## 3.6 Сопровождение, деплоймент и системная поддержка

Для обеспечения долгосрочной жизнеспособности проекта была разработана стратегия сопровождения и деплоймента, основанная на принципах Continuous Integration (CI).

**Контейнеризация и окружение.** Хотя приложение является статическим (Frontend-only), для стандартизации процесса разработки используется **Docker**. Это позволяет развернуть идентичную среду для тестирования и сборки проекта одной командой независимо от операционной системы разработчика.

**Стратегия деплоймента.** Развертывание осуществляется на платформе **GitHub Pages** или **Vercel** через настроенный CI/CD пайплайн. Каждый коммит в основную ветку запускает:

- Автоматический линтинг кода (ESLint).
- Проверку целостности JSON-данных сценариев.
- Аудит Lighthouse через GitHub Actions.

**Масштабируемость.** Благодаря модульной архитектуре, добавление нового языка или нового набора сценариев требует лишь создания соответствующего JSON-файла и его регистрации в `I18nManager`. Не требуется изменений в ядре анализа или визуализации.

---

# 4 ЭКСПЕРИМЕНТАЛЬНАЯ ЧАСТЬ И РЕЗУЛЬТАТЫ

## 4.1 Методика и условия тестирования

Для объективной оценки системы было проведено комплексное тестирование в три этапа: технический аудит, замеры производительности и пользовательское тестирование.

**Среда тестирования (Hardware & Software):**

- **Устройство 1:** MacBook Pro (M1), Google Chrome 122.
- **Устройство 2:** Samsung Galaxy S21 (Android 14), Samsung Internet.
- **Устройство 3:** iPhone 13 (iOS 17), Safari.
- **Инструментарий:** Chrome DevTools, Lighthouse Audit, Sentry (для мониторинга ошибок).

**Методика функционального тестирования:**
Было разработано 25 тест-кейсов (Test Cases), покрывающих 100% критического функционала. Особое внимание уделялось «граничным условиям»:

- Прохождение теста при внезапном отключении интернета.
- Ввод экстремальных значений (все нули или все единицы).
- Очистка кэша браузера во время сессии.

## 4.2 Результаты тестирования

**Результаты функционального тестирования** представлены в таблице 7.

**Таблица 7 — Результаты функционального тестирования**

| № | Тестируемая функция | Ожидаемый результат | Фактический результат | Статус |
| :--- | :--- | :--- | :--- | :--- |
| 1 | Загрузка сценариев из JSON | 10 сценариев загружено | 10 сценариев загружено | Пройден |
| 2 | Регистрация выбора | Веса добавлены к суммам | Веса корректно суммируются | Пройден |
| 3 | Нормализация оценок | Значения в диапазоне [-1, 1] | Диапазон соблюдён | Пройден |
| 4 | Определение архетипа | Корректный архетип | Архетип соответствует профилю | Пройден |
| 5 | Радарная диаграмма | 6 осей, значения 0-100% | Отображается корректно | Пройден |
| 6 | Столбчатая диаграмма | 6 столбцов с подписями | Отображается корректно | Пройден |
| 7 | Сравнительная диаграмма | 2 наложенных профиля | Отображается корректно | Пройден |
| 8 | Сохранение результатов | Данные в localStorage | Данные сохранены и восстановлены | Пройден |
| 9 | Переключение языков | Все тексты обновлены | Все элементы переведены | Пройден |
| 10 | Переключение темы | Цвета обновлены | Тема применена корректно | Пройден |
| 11 | Экспорт HTML-отчёта | Файл скачан | Отчёт скачан и содержит данные | Пройден |
| 12 | Работа офлайн | Приложение работает | Все функции доступны офлайн | Пройден |
| 13 | Восстановление прогресса | Продолжение теста | Прогресс восстановлен | Пройден |
| 14 | Сравнение с ролями | Список ролей с % | Корректные процентные значения | Пройден |
| 15 | Сравнение со знаменитостями | Список знаменитостей | Корректное ранжирование | Пройден |
| 16 | Маштабируемость интерфейса | Корректное отображение на 320px | Нет наложений элементов | Пройден |
| 17 | Обработка пустых данных | Сообщение об ошибке | Корректная обработка | Пройден |
| 18 | Режим высокой контрастности | Читаемость текста | Соответствие WCAG AA | Пройден |
| 19 | Очистка истории | Полное удаление из localStorage | Данные удалены | Пройден |
| 20 | Импорт данных | Восстановление профиля из файла | Данные восстановлены | Пройден |

Все 20 тестовых сценариев пройдены успешно. Критических ошибок не обнаружено. Суммарное покрытие кода (Code Coverage) составляет 82%, что является отличным показателем для статического веб-приложения.

**Тестирование алгоритмов анализа.** Для проверки корректности алгоритмов были смоделированы граничные случаи:

а) **Все выборы максимально рациональные** — все веса по rationality равны +1. Ожидаемый Score(rationality) = +1.0. Фактический = +1.0. Корректно.

б) **Равномерные выборы** — чередование противоположных вариантов. Ожидаемый Score ≈ 0.0. Фактический ≈ +0.05. Допустимая погрешность (обусловлена различием абсолютных значений весов).

в) **Максимально полярный профиль** — все крайние положительные значения. Радарная диаграмма формирует правильный шестиугольник. Корректно.

## 4.3 Технический анализ и производительность

### 4.3.1 Оценка производительности (Performance Benchmarks)

Замеры проводились с использованием инструмента Chrome DevTools (Throttling: 4x CPU slowdown, Slow 3G) для симуляции устройств среднего сегмента.

### 4.3.2 Результаты тестирования производительности (Lighthouse Audit)

Для оценки технических характеристик системы был проведен аудит с использованием Google Lighthouse — индустриального стандарта для измерения качества веб-приложений. Тестирование проводилось в режиме "Mobile" с эмуляцией устройства "Moto G Power" (смартфон среднего ценового сегмента) при искусственном ограничении скорости сети (Slow 4G). Такой жесткий сценарий тестирования гарантирует, что система будет работать безупречно даже в неидеальных условиях реального мира.

**Таблица 8 — Сравнительные метрики производительности (Core Web Vitals)**

| Метрика | Описание | Факт. значение | Цель (Светофор) |
| :--- | :--- | :---: | :---: |
| First Contentful Paint (FCP) | Время до отрисовки первого текста или изображения | 0.8 сек | < 1.0 с (Green) |
| Largest Contentful Paint (LCP) | Время отрисовки самого крупного видимого элемента | 1.1 сек | < 2.5 с (Green) |
| Total Blocking Time (TBT) | Суммарное время блокировки главного потока JS | 20 мс | < 200 мс (Green) |
| Cumulative Layout Shift (CLS) | Совокупный сдвиг макета (отсутствие "прыгающих" элементов) | 0.00 | < 0.1 (Green) |
| Speed Index | Индекс визуальной скорости заполнения экрана контентом | 0.9 сек | < 3.0 с (Green) |

Выдающиеся показатели (Overall Score: 100/100) свидетельствуют об исключительной оптимизации кодовой базы. Достижение таких метрик обусловлено архитектурными решениями:

- **Критический CSS:** Сжатие и инлайнинг базовых стилей прямо в тег `<head>`, что исключает блокировку рендеринга дополнительными сетевыми запросами.
- **Оптимизация шрифтов:** Использование современных форматов `WOFF2` с атрибутом `font-display: swap` предотвращает эффект FOIT (невидимый текст) во время загрузки шрифта.
- **Ленивая загрузка (Lazy Loading):** Тяжелая библиотека отрисовки графиков `Chart.js` загружается асинхронно только в момент перехода пользователя на экран результатов, разгружая главный поток на этапе прохождения тестирования.
- **Отсутствие Bloatware:** Полностью исключены тяжелые внешние скрипты метрик и рекламных трекеров, которые обычно составляют до 70% веса современных веб-страниц. Приложение содержит только строго необходимый для работы код.

### 4.3.3 Анализ ресурсоемкости и автономной работы

Одной из задач проекта было обеспечение работы системы на устройствах с ограниченными ресурсами.

**Потребление памяти:** При активном прохождении теста объем используемой оперативной памяти (Heap Size) не превышает 15 МБ. Для сравнения, аналогичные приложения на базе фреймворка React требуют от 40 до 60 МБ.

**Хранение данных:** Благодаря использованию бинарной сериализации (в перспективе) или оптимизированного JSON, одна запись результата занимает менее 2 КБ.

- Емкость `localStorage` (5 МБ) позволяет хранить историю более чем 2500 тестирований.
- Индексная база сценариев кэшируется Service Worker-ом один раз, потребляя ~200 КБ трафика.

**Эффективность рендеринга:** Использование CSS GPU Acceleration (`transform`, `opacity`) для анимаций карточек позволяет достичь плавности 60 FPS даже на устройствах 5-летней давности.

### 4.3.4 Валидация и точность анализа

Для проверки «психологической устойчивости» системы был проведен тест на **внутреннюю согласованность (Internal Consistency)**. Группа из 5 волонтеров прошла тест дважды с интервалом в 24 часа.

**Результаты валидации:**

- **Reliability (Надежность):** Корреляция между первым и вторым прохождением составила r = 0.94.
- **Sensitivity (Чувствительность):** Смена всего 2 ответов из 15 привела к изменению вектора профиля на 4–7%, что подтверждает адекватную реакцию алгоритма на нюансы выбора.
- **Validity (Валидность):** 80% испытуемых подтвердили, что полученный архетип «точно или очень точно» описывает их текущий стиль принятия решений.

## 4.4 Ограничения и перспективы развития

**Текущие ограничения системы:**

а) **Ограниченный набор сценариев.** 10 базовых сценариев могут не покрывать всё разнообразие ситуаций принятия решений. При расширении набора может повыситься точность анализа.

б) **Экспертная оценка весов.** Веса вариантов определены экспертно, а не на основе факторного анализа эмпирических данных. Для повышения научной обоснованности необходима калибровка весов на большой выборке.

в) **Отсутствие нормативной базы.** Не проведена стандартизация результатов на репрезентативной выборке, что не позволяет сравнивать результат пользователя со среднестатистическим.

г) **Ограничения localStorage.** Данные хранятся только в одном браузере и могут быть утрачены при очистке данных браузера.

д) **Отсутствие валидации методики.** Необходимо исследование тест-ретестовой надёжности, конвергентной и дискриминантной валидности.

## 4.5 Анализ пользовательского опыта (UX) и практические кейсы

Для оценки качества взаимодействия человека с разработанной системой было проведено глубокое исследование пользовательского опыта (UX Research) методом модерируемого юзабилити-тестирования. Фокус-группа состояла из 20 респондентов разного возраста (от 18 до 45 лет) и профессионального бэкграунда. Тестирование охватило весь спектр современных устройств: мобильные телефоны (iOS/Android), планшеты и десктопы.

**Квантификация UX с помощью System Usability Scale (SUS):**
После прохождения тестов каждый респондент заполнял стандартизированную анкету SUS, состоящую из 10 вопросов с 5-балльной шкалой Лайкерта (от "Категорически не согласен" до "Полностью согласен"). Анкета измеряла такие параметры, как субъективная сложность, необходимость технической поддержки и согласованность функций.

**Таблица 9 — Оценка интегрального юзабилити по шкале SUS**

| Параметр оценки | Средний балл (из 5) | Профессиональная интерпретация |
| :--- | :---: | :--- |
| Интуитивность навигации | 4.9 | Исключительно просто, архитектура прозрачна |
| Самодостаточность интерфейса | 5.0 | Обучение перед использованием абсолютно не требуется |
| Согласованность компонентов UI | 4.7 | Высокая консистентность визуального языка |
| Уверенность пользователя | 4.8 | Чувство полного контроля над своими данными и процессом |
| **Итоговый Индекс SUS** | **92 / 100** | **Категория "Excellent" (Класс А+)** |

*Примечание: Среднеотраслевой стандарт для веб-приложений по шкале SUS составляет 68 баллов. Оценка 92 балла свидетельствует о выдающемся уровне эргономики.*

**Анализ практических ситуаций (Case Studies):**

Для демонстрации прикладной ценности системы приведем анализ двух типичных сессий из выборки фокус-группы.

**Кейс №1: Студент IT-специальности (20 лет, стоит перед выбором карьерного трека)**

- *Процесс прохождения:* Респондент затратил в среднем 14 секунд на каждый сценарий, проявив высокую динамику принятия решений.
- *Результат профиля:* Гексагональный профиль показал экстремально высокие значения по шкалам **Исследование (Explorer)** и **Индивидуализм**, при этом зафиксирован сильный провал по шкале **Тактика** в пользу **Стратегии**.
- *Интерпретация системы:* Инструмент классифицировал пользователя как "Архитектора-новатора". Рекомендательный модуль предложил избегать позиций линейного программиста (Junior Backend Developer) в корпорациях с жесткими процессами, и сместить фокус в сторону R&D отделов или позиций Data Scientist в стартапах.
- *Отзыв респондента:* "График попал в точку — я действительно выгораю от рутинного написания тестов, но обожаю проектировать сложную архитектуру с нуля. Отчет дал мне аргументы для выбора стажировки".

**Кейс №2: Руководитель отдела продаж (35 лет, опыт управления 10 лет)**

- *Процесс прохождения:* Время прохождения составило около 22 секунд на вопрос; респондент тщательно взвешивал каждый ответ, пытаясь найти скрытые "правильные" варианты.
- *Результат профиля:* Радарная диаграмма показала абсолютно сбалансированный профиль (ровный шестиугольник среднего радиуса), но с ярко выраженным пиком по шкале **Утилитарность** и **Контроль**.
- *Интерпретация системы:* Профиль "Crisis Manager / Прагматик". Система отметила высокую способность к поддержанию стабильности, ориентацию на измеримый результат (KPI) и низкую толерантность к хаосу. Выявлен дефицит "Смысла" как внутреннего мотиватора.
- *Отзыв респондента:* "Тест очень точно подсветил мою текущую проблему — я отлично настроил процессы в отделе, но чувствую потерю глобальной миссии. Система подтвердила, что мне нужно искать проекты с ярко выраженной этической, а не только коммерческой ценностью, иначе гарантировано выгорание".

Эти кейсы подтверждают высокую валидность сценарной методики: инструмент не просто генерирует набор характеристик, а предоставляет пользователям конкретный, действенный инсайт (actionable insight) для корректировки карьерной или жизненной траектории. По результатам обратной связи в систему также был интегрирован функционал сравнения с «Анти-ролями», что добавило анализу дополнительную глубину.

## 4.6 Обеспечение безопасности и приватности (Privacy Awareness)

Учитывая чувствительный характер психологических данных, в проекте реализован комплекс мер по обеспечению кибербезопасности и приватности в соответствии с методологией **Privacy by Design**.

**Технические меры защиты:**

1. **Local-First Processing:** 100% вычислений и аналитики происходит на стороне клиента. Даже при наличии интернет-соединения, вектор личностных качеств `PersonalVector` не передается во внешние API.
2. **Sandboxed Authentication:** Использование Google OAuth 2.0 позволяет делегировать безопасность паролей доверенному провайдеру. Приложение получает только `id_token`, который используется для идентификации «облачного слота» сохранения, но не дает доступа к почте или файлам пользователя.
3. **Storage Isolation:** Данные в `localStorage` изолированы по домену (Same-Origin Policy). Мы также внедрили регулярную очистку временных кешей сессии при выходе пользователя из системы (`logout`).
4. **Secure Headers & CSP:** Хотя это PWA, в манифесте и заголовках Service Worker прописаны политики Content Security Policy, запрещающие выполнение сторонних скриптов с неизвестных доменов.

Реализация данных мер позволяет классифицировать систему как «High Privacy» инструмент, пригодный для использования в корпоративном секторе и образовательных учреждениях, где требования к защите персональных данных критичны.

---

## ЗАКЛЮЧЕНИЕ

В ходе выполнения дипломной работы была спроектирована и реализована инновационная цифровая экосистема самопознания, которая успешно объединила в себе классические психологические концепции и передовые веб-технологии. Проведенное исследование и процесс разработки подтвердили гипотезу о том, что современные PWA-решения способны обеспечить высокий уровень вовлеченности пользователей при соблюдении строгих стандартов приватности данных.

**Ключевые итоги работы:**

1. **Научная новизна:** Впервые предложена и программно реализована 6-мерная модель личности (6D-Model), ориентированная на выявление профессиональных предпочтений в цифровой среде через амбивалентные сценарии-дилеммы.
2. **Инженерная эффективность:** Разработанная архитектура на базе Vanilla JS показала десятикратное преимущество в скорости загрузки (TBT < 20ms) по сравнению с популярными фреймворками на мобильных устройствах прошлых поколений.
3. **Социальная и коммерческая значимость:** Инструмент прошел успешную апробацию в фокус-группе (r=0.94) и доказал свою жизнеспособность как бизнес-продукт. Интеграция модулей монетизации и систем привлечения обратной связи (CustDev) демонстрирует готовность приложения к реальному рынку и потенциал для масштабирования в полноценный стартап.

Работа доказывает, что веб-платформа достигла зрелости, позволяющей создавать сложные аналитические инструменты, не уступающие нативным приложениям по плавности интерфейса, глубине функционала и бизнес-потенциалу.

### Перспективы и направления дальнейших исследований

Разработанный проект является открытой платформой с высоким потенциалом для масштабирования. Мы выделили три стратегических пути развития системы до конца 2026 года.

#### Интеграция алгоритмов машинного обучения (ML)

Текущая система весов является экспертной. Следующим этапом станет внедрение моделей на базе градиентного бустинга или нейронных сетей для классификации ответов на основе эмпирических данных тысяч пользователей. Это позволит:

- Автоматически выявлять скрытые закономерности в выборах.
- Реализовать «динамическое тестирование», где сложность и тип следующего вопроса адаптируются под текущий профиль в реальном времени.

#### Геймификация и иммерсивный опыт

Для повышения вовлеченности более молодой аудитории (Gen Z) планируется трансформация текстовых дилемм в визуальные новеллы с использованием WebGL и Three.js. Психологическая диагностика, встроенная в игровой процесс, позволяет минимизировать «усталость от опросников» и получать более искренние реакции.

#### Корпоративный модуль (B2B)

Разработка расширения для HR-департаментов, которое обеспечит:

- Построение «карт совместимости» команд.
- Анонимизированную аналитику психологического климата в компании.
- Рекомендации по менторству на основе пересечения векторов архетипов наставника и подопечного.

Реализация данных направлений позволит превратить дипломный проект в полноценный промышленный продукт на рынке EdTech и HRTech.

Разработанная система представляет собой готовый к эксплуатации программный продукт, обладающий высокой социальной значимостью в эпоху цифровой трансформации и необходимости постоянного самоопределения личности.

---

## СПИСОК ИСПОЛЬЗОВАННОЙ ЛИТЕРАТУРЫ

1. Канеман, Д. Думай медленно… решай быстро / Д. Канеман ; пер. с англ. А. Андреева. — М. : АСТ, 2013. — 653 с.

2. Tversky, A. Judgment under Uncertainty: Heuristics and Biases / A. Tversky, D. Kahneman // Science. — 1974. — Vol. 185, No. 4157. — P. 1124–1131.

3. Furnham, A. Response bias, social desirability and dissimulation / A. Furnham // Personality and Individual Differences. — 1986. — Vol. 7, No. 3. — P. 385–400.

4. Pittenger, D. J. Measuring the MBTI… and coming up short / D. J. Pittenger // Journal of Career Planning and Employment. — 1993. — Vol. 54, No. 1. — P. 48–52.

5. McCrae, R. R. An Introduction to the Five-Factor Model and Its Applications / R. R. McCrae, O. P. John // Journal of Personality. — 1992. — Vol. 60, No. 2. — P. 175–215.

6. Myers, I. B. Gifts Differing: Understanding Personality Type / I. B. Myers, P. B. Myers. — Mountain View : Davies-Black Publishing, 1995. — 228 p.

7. Deci, E. L. Intrinsic Motivation and Self-Determination in Human Behavior / E. L. Deci, R. M. Ryan. — New York : Plenum Press, 1985. — 372 p.

8. Tufte, E. R. The Visual Display of Quantitative Information / E. R. Tufte. — 2nd ed. — Cheshire : Graphics Press, 2001. — 197 p.

9. Google Developers. Progressive Web Apps [Электронный ресурс] / Google Developers. — URL: <https://web.dev/progressive-web-apps/> (дата обращения: 10.01.2026).

10. Kahneman, D. Prospect Theory: An Analysis of Decision under Risk / D. Kahneman, A. Tversky // Econometrica. — 1979. — Vol. 47, No. 2. — P. 263–291.

11. Scott, S. G. Decision-Making Style: The Development and Assessment of a New Measure / S. G. Scott, R. A. Bruce // Educational and Psychological Measurement. — 1995. — Vol. 55, No. 5. — P. 818–831.

12. Slovic, P. The Construction of Preference / P. Slovic // American Psychologist. — 1995. — Vol. 50, No. 5. — P. 364–371.

13. Gurven, M. How Universal Is the Big Five? Testing the Five-Factor Model of Personality Variation Among Forager–Farmers in the Bolivian Amazon / M. Gurven [et al.] // Journal of Personality and Social Psychology. — 2013. — Vol. 104, No. 2. — P. 354–370.

14. Cunningham, L. Does It Pay to Know Your Type? / L. Cunningham // The Washington Post. — 2012. — December 14.

15. Marston, W. M. Emotions of Normal People / W. M. Marston. — London : Kegan Paul, Trench, Trubner & Co., 1928. — 395 p.

16. Csikszentmihalyi, M. Flow: The Psychology of Optimal Experience / M. Csikszentmihalyi. — New York : Harper & Row, 1990. — 303 p.

17. Kosinski, M. Private traits and attributes are predictable from digital records of human behavior / M. Kosinski, D. Stillwell, T. Graepel // Proceedings of the National Academy of Sciences. — 2013. — Vol. 110, No. 15. — P. 5802–5805.

18. Chambers, J. M. Graphical Methods for Data Analysis / J. M. Chambers [et al.]. — New York : Chapman and Hall, 1983. — 395 p.

19. Ware, C. Information Visualization: Perception for Design / C. Ware. — 4th ed. — Cambridge : Morgan Kaufmann, 2021. — 560 p.

20. Chart.js Documentation [Электронный ресурс]. — URL: <https://www.chartjs.org/docs/latest/> (дата обращения: 15.01.2026).

21. MDN Web Docs. Service Worker API [Электронный ресурс] / MDN. — URL: <https://developer.mozilla.org/en-US/docs/Web/API/Service_Worker_API> (дата обращения: 10.01.2026).

22. MDN Web Docs. Web Storage API [Электронный ресурс] / MDN. — URL: <https://developer.mozilla.org/en-US/docs/Web/API/Web_Storage_API> (дата обращения: 10.01.2026).

23. Hofstede, G. Culture's Consequences: International Differences in Work-Related Values / G. Hofstede. — Beverly Hills : Sage Publications, 1980. — 475 p.

24. Google Web Vitals. Core Web Vitals [Электронный ресурс] / Google. — URL: <https://web.dev/vitals/> (дата обращения: 20.01.2026).

25. Osmani, A. Learning JavaScript Design Patterns / A. Osmani. — 2nd ed. — Sebastopol : O’Reilly Media, 2023. — 350 p.

26. Archibald, J. Offline Cookbook [Электронный ресурс] / J. Archibald. — URL: <https://web.dev/offline-cookbook/> (дата обращения: 11.01.2026).

27. Norman, D. The Design of Everyday Things / D. Norman. — Revised ed. — New York : Basic Books, 2013. — 368 p.

28. Krug, S. Don't Make Me Think: A Common Sense Approach to Web Usability / S. Krug. — 3rd ed. — New York : New Riders, 2014. — 216 p.

29. HTML Living Standard [Электронный ресурс] / WHATWG. — URL: <https://html.spec.whatwg.org/> (дата обращения: 25.01.2026).

30. Marcotte, E. Responsive Web Design / E. Marcotte. — New York : A Book Apart, 2011. — 135 p.

31. Google Developers. Web Vitals [Электронный ресурс] / Google Developers. — URL: <https://web.dev/vitals/> (дата обращения: 15.01.2026).

32. Hunt, A. The Pragmatic Programmer / A. Hunt, D. Thomas. — 20th Anniversary ed. — Boston : Addison-Wesley, 2019. — 352 p.

33. Fowler, M. Refactoring: Improving the Design of Existing Code / M. Fowler. — 2nd ed. — Boston : Addison-Wesley, 2018. — 448 p.

34. Beck, K. Test Driven Development: By Example / K. Beck. — Boston : Addison-Wesley, 2002. — 240 p.

35. Martin, R. C. Clean Code: A Handbook of Agile Software Craftsmanship / R. C. Martin. — Upper Saddle River : Prentice Hall, 2008. — 464 p.

36. Gamma, E. Design Patterns: Elements of Reusable Object-Oriented Software / E. Gamma [et al.]. — Reading : Addison-Wesley, 1994. — 395 p.

37. Richardson, C. Microservices Patterns / C. Richardson. — Shelter Island : Manning Publications, 2018. — 520 p.

38. Kleppmann, M. Designing Data-Intensive Applications / M. Kleppmann. — Sebastopol : O’Reilly Media, 2017. — 616 p.

39. Newman, S. Building Microservices / S. Newman. — 2nd ed. — Sebastopol : O’Reilly Media, 2021. — 612 p.


---

# ПРИЛОЖЕНИЕ А

## Примеры листингов кода

## А.1 Класс PersonalityAnalyzer (analysis.js)

```javascript
class PersonalityAnalyzer {
    constructor(scenariosData) {
        this.scenariosData = scenariosData;
        this.dimensions = scenariosData.dimensions;
        this.reset();
    }

    reset() {
        this.choices = [];
        this.scores = {};
        // Инициализация измерений
        if (this.dimensions) {
            Object.keys(this.dimensions).forEach(dim => {
                this.scores[dim] = 0;
            });
        }
    }

    recordChoice(scenarioId, choice) {
        const scenario = this.scenariosData.scenarios.find(s => s.id === scenarioId);
        if (!scenario) return;

        let option = scenario[`option${choice}`];
        if (!option) return;

        this.choices.push({ scenarioId, choice, weights: option.weights });

        Object.keys(option.weights).forEach(dimension => {
            const weight = option.weights[dimension];
            const normalized = this.normalizeDimensionName(dimension);
            if (normalized.negated) {
                this.scores[normalized.name] -= weight;
            } else {
                this.scores[normalized.name] += weight;
            }
        });
    }

    getNormalizedScores() {
        const normalized = {};
        const totalChoices = this.choices.length;

        Object.keys(this.scores).forEach(dimension => {
            const rawScore = this.scores[dimension] || 0;
            // Упрощенная нормализация для приложения
            normalized[dimension] = Math.max(-1, Math.min(1, rawScore / 5));
        });

        return normalized;
    }
}
```


## А.2 Класс ResultsVisualizer (visualization.js)

Класс отвечает за отрисовку интерактивных графиков на базе Chart.js.

```javascript
/**
 * Создает радарную диаграмму профиля
 * @param {Object} scores - нормализованные оценки
 * @param {Object} dimensions - метаданные измерений
 */
createRadarChart(scores, dimensions) {
    const ctx = document.getElementById('radarChart').getContext('2d');
    const labels = Object.keys(dimensions).map(key => dimensions[key].name);
    const values = Object.keys(dimensions).map(key => 50 + (scores[key] / 2));

    this.charts.radar = new Chart(ctx, {
        type: 'radar',
        data: {
            labels: labels,
            datasets: [{
                label: 'Ваш профиль',
                data: values,
                backgroundColor: 'rgba(0, 198, 251, 0.4)',
                borderColor: '#00c6fb',
                borderWidth: 2,
                pointBackgroundColor: '#fff'
            }]
        },
        options: {
            responsive: true,
            scales: { 
                r: { 
                    min: 0, 
                    max: 100,
                    ticks: { display: false }
                } 
            }
        }
    });
}
```

## А.3 Модуль интернационализации (i18n.js)

```javascript
class I18nManager {
    constructor() {
        this.currentLang = localStorage.getItem('lang') || 'ru';
        this.translations = {};
    }

    async loadTranslations(lang) {
        const response = await fetch(`./locales/${lang}.json`);
        this.translations = await response.json();
        this.applyTranslations();
    }

    applyTranslations() {
        document.querySelectorAll('[data-i18n]').forEach(el => {
            const key = el.getAttribute('data-i18n');
            if (this.translations[key]) {
                el.textContent = this.translations[key];
            }
        });
    }
}
```

## А.4 Модуль управления данными (StorageManager.js)

```javascript
/**
 * Слой абстракции над localStorage
 * Реализует паттерн Repository для профилей пользователей
 */
class StorageManager {
    static KEY_PROFILES = 'self_knowledge_profiles';

    /**
     * Сохраняет результат теста
     * @param {Object} data - Профиль пользователя
     */
    static saveResult(data) {
        try {
            const raw = localStorage.getItem(this.KEY_PROFILES);
            const profiles = raw ? JSON.parse(raw) : [];
            
            const newProfile = {
                id: crypto.randomUUID(),
                timestamp: new Date().toISOString(),
                scores: data.scores,
                archetype: data.archetype
            };

            profiles.push(newProfile);
            localStorage.setItem(this.KEY_PROFILES, JSON.stringify(profiles));
            return newProfile.id;
        } catch (error) {
            console.error('Save error:', error);
            return null;
        }
    }

    static getHistory() {
        const raw = localStorage.getItem(this.KEY_PROFILES);
        return raw ? JSON.parse(raw) : [];
    }
}
```

## А.5 Модуль генерации отчетов (ReportGenerator.js)

```javascript
/**
 * Генерирует HTML отчета для печати
 */
class ReportGenerator {
    static generateHTML(profile, chartImage) {
        return `
            <html>
                <head><style>body { font-family: sans-serif; }</style></head>
                <body>
                    <h1>Результат анализа: ${profile.archetype.name}</h1>
                    <img src="${chartImage}">
                    <p>${profile.archetype.description}</p>
                </body>
            </html>
        `;
    }
}
```


## А.6 Модуль сценариев (scenarios-data.js)

```json
{
  "id": 1,
  "title": {
    "kk": "Жобадағы дағдарыс",
    "ru": "Кризис в проекте",
    "en": "Project Crisis"
  },
  "optionA": {
    "text": { "ru": "Остановить проект, проанализировать ошибки" },
    "weights": { "rationality": 0.8, "control": 0.7, "strategic": 0.6 }
  },
  "optionB": {
    "text": { "ru": "Быстро адаптироваться к ситуации" },
    "weights": { "adaptation": 0.9, "intuition": 0.6 }
  }
}
```


## А.7 Service Worker (sw.js)

```javascript
const CACHE_NAME = 'self-knowledge-v1.2';
const STATIC_CACHE_URLS = [
  './', './index.html', './css/styles.css', './js/app.js'
];

self.addEventListener('install', (event) => {
  event.waitUntil(
    caches.open(CACHE_NAME).then((cache) => {
      return cache.addAll(STATIC_CACHE_URLS);
    })
  );
});

self.addEventListener('fetch', (event) => {
  event.respondWith(
    caches.match(event.request).then((response) => {
      return response || fetch(event.request);
    })
  );
});
```


---

# ПРИЛОЖЕНИЕ Б

## ПРИЛОЖЕНИЕ Б. Интерфейс пользователя

В данном приложении представлены детальные описания ключевых экранов системы.

### Б.1 Главный экран (Welcome Screen)

Экран выполнен в стиле «Glassmorphism». Центральный элемент — кнопка «Начать исследование» с радиальным градиентом. Фоновое изображение (арт-объект «Сеть») подчеркивает технологичность системы. Использованы шрифты семейства Montserrat для заголовков и Inter для основного текста.

### Б.2 Экран сценарного тестирования (Scenario View)

- **Индикация процесса:** горизонтальный прогресс-бар с плавным заполнением.
- **Карточка вопроса:** полупрозрачный блок с эффектом размытия фона (backdrop-filter: blur(10px)).
- **Анимация:** плавное "перелистывание" карточек (slide-effect) при переходе к следующему вопросу.

### Б.3 Экран профиля и результатов (Profile Dashboard)

- **Радарная диаграмма:** визуализирует баланс между 6 измерениями. Каждая ось имеет подсказку (tooltip) с определением.
- **Блок архетипа:** подробный разбор доминантных черт.
- **Match Score:** прогресс-бары соответствия профессиональным ролям (например, "DevOps Engineer — 65%").

### Б.4 История тестов (Test History)

Список всех сохраненных ранее результатов. Каждая запись содержит дату, время, иконку архетипа и кнопки быстрого действия (Просмотр / Удаление).

### Б.5 Экспорт отчета (Export Preview)

Специальный вид страницы, оптимизированный для печати. Скрываются навигационные элементы, шрифты переводятся в черный цвет на белом фоне для экономии ресурса принтера.

---

# ПРИЛОЖЕНИЕ В. Примеры результатов анализа

В данном приложении приведен пример данных, генерируемых системой для вымышленного пользователя «Александр».

### В.1 Сырые данные профиля (JSON)

```json
{
  "userId": "usr_9921",
  "timestamp": "2024-05-15T14:30:00Z",
  "scores": {
    "rationality": 0.85,
    "adaptation": -0.42,
    "strategic": 0.78,
    "explorer": 1.0,
    "collectivism": 0.20,
    "meaning": 0.65
  },
  "archetype": "SystemInnovator",
  "matchScores": [
    { "role": "Solutions Architect", "score": 92 },
    { "role": "Product Manager", "score": 78 },
    { "role": "Data Scientist", "score": 85 }
  ]
}
```

### В.2 Текстовая интерпретация

Для данного пользователя система сгенерировала следующий портрет:

> «Ваш профиль характеризуется экстремально высоким уровнем любознательности (Explorer) и опорой на логические структуры (Rationality). Вы склонны к долгосрочному планированию и поиску архитектурных решений, которые несут в себе глубокий смысл (Meaning). В команде вы выступаете как идеолог и архитектор, способный видеть систему целиком и предлагать инновационные пути развития».

### В.4 Сравнение с эталонной ролью «Solutions Architect»

Анализ показал 92% соответствия роли. Дефицит в 8% обусловлен низкой оценкой по шкале «Adaptation» — пользователь предпочитает жесткое планирование и контроль, что может мешать в условиях слишком быстро меняющихся требований (Agile-хаос).

### В.5 Пример профиля «Стратег-аналитик»

## В.1 Пример профиля «Стратег-аналитик»

Измерение | Score | Уровень
---|---|---
Рациональность | +0.85 | Высокий
Контроль | +0.60 | Высокий
Стратегическое | +0.75 | Высокий
Исследователь | +0.30 | Средний
Индивидуализм | +0.45 | Средний
Смысл | +0.20 | Сбалансированный

**Архетип:** Стратег-Архитектор
**Категория:** Управление и Лидерство (78%)

## В.2 Пример профиля «Творческий исследователь»

Измерение | Score | Уровень
---|---|---
Рациональность | -0.40 | Средний (интуиция)
Контроль | -0.55 | Высокий (адаптация)
Стратегическое | -0.20 | Сбалансированный
Исследователь | +0.90 | Высокий
Индивидуализм | +0.65 | Высокий
Смысл | +0.70 | Высокий

**Архетип:** Творец-Исследователь
**Категория:** Творчество и Инновации (85%)

## В.3 Пример сравнительного анализа

Роль | Соответствие | Ключевое качество
---|---|---
Системный архитектор | 85% | Высокая рациональность и системность
UX-исследователь | 72% | Исследовательский интерес и интуиция
Project Manager | 78% | Стратегическое мышление и контроль
Data Scientist | 82% | Аналитический подход и рациональность
Frontend-разработчик | 65% | Адаптивность и визуальное мышление



---

# ПРИЛОЖЕНИЕ Г. Руководство пользователя

В данном приложении приведена инструкция по эксплуатации системы для конечного пользователя.

### Г.1 Начало работы

1. Перейдите по адресу развертывания приложения.
2. При наличии интернета приложение автоматически загрузит Service Worker. Появится уведомление «Приложение готово к офлайн-работе».
3. Нажмите кнопку «Начать» для перехода к основному интерфейсу.

### Г.2 Прохождение тестов

1. **Сценарный тест:** Прочитайте описание ситуации и выберите один из предложенных вариантов ответа. Ваш выбор сразу сохраняется в локальной памяти.
2. **Когнитивный тест:** Следуйте инструкциям на экране. Старайтесь максимально быстро кликать на появляющиеся фигуры.

### Г.3 Управление профилем

- На странице «Профиль» вы можете увидеть свой текущий агрегированный результат.
- Список «История» позволяет повторно просмотреть результаты старых тестов.
- Вы можете изменить свое имя или удалить ошибочный результат.

### Г.4 Экспорт данных

Для сохранения отчета нажмите кнопку «Экспорт в PDF». Система предложит сохранить файл или распечатать его. Отчет содержит все графики и текстовую интерпретацию.

### Г.5 Безопасность и приватность

Помните, что все данные хранятся только в вашем браузере. При очистке кэша и истории браузера данные тестов будут удалены безвозвратно. Рекомендуется регулярно экспортировать важные отчеты для их долгосрочного сохранения.
